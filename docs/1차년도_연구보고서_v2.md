# [연구 목표 및 내용] - 1차년도

**프로젝트명**: 굴착기 제조 공정 생산성 향상을 위한 AI 기반 부품 인식 및 위치 추정 시스템  
**작성일**: 2025년 12월 30일  
**프로젝트 저장소**: `/home/rebirther/isaac_data_output`

---

## 1. 굴착기 제조 공정 생산성 향상을 위한 공정 프로세스 기술 조사

### 1.1 굴착기 제조 공정의 현장 환경 및 흐름 조사

> ※ 본 항목은 현장 조사가 필요한 부분으로, 코드베이스에서 직접 도출하기 어렵습니다.  
> 아래는 시스템 설계 시 고려한 공장 환경 시뮬레이션 내용입니다.

**시뮬레이션 환경에서 반영한 현장 조건**:

| 환경 요소 | 시뮬레이션 반영 내용 |
|-----------|---------------------|
| 바닥 환경 | 회색~갈색 계열 (콘크리트/철판) |
| 벽면 환경 | 회색~베이지 계열 (공장 벽면) |
| 조명 조건 | 다양한 위치의 점광원 (공장 조명 시뮬레이션) |
| 카메라 시점 | 사선 각도 (측면+윗면 동시 촬영 가능) |

### 1.2 주요 부품의 제작 공정 프로세스 기술 분석

> ※ 본 항목은 현장 조사가 필요한 부분입니다.

**대상 부품 목록** (AI 인식 대상):

| 부품명 | 규격 | 용도 |
|--------|------|------|
| arm_link_25_nomat | 25톤급 | 암 링크 부품 |
| arm_link_30_notmat | 30톤급 | 암 링크 부품 |
| boom_link_25_nomat | 25톤급 | 붐 링크 부품 |
| boom_link_30_notmat | 30톤급 | 붐 링크 부품 |

---

## 2. 부품 인식 및 위치 추정 데이터 항목 정의 및 AI 모델 구축을 위한 기술조사

### 2.1 공정 흐름 기반의 부품 인식 및 위치 추정을 위한 데이터 항목 정의

#### 2.1.1 입력 데이터 항목

| 데이터 항목 | 형식 | 크기 | 설명 |
|-------------|------|------|------|
| RGB 이미지 | PNG | 1024×1024 | 부품이 촬영된 컬러 이미지 |
| Depth 맵 | NPY (float32) | 1024×1024 | 카메라로부터의 거리 (미터 단위) |
| 2D Bounding Box | NPY + JSON | 가변 | 이미지 내 부품 영역 좌표 |

#### 2.1.2 출력 데이터 항목 (라벨)

| 데이터 항목 | 형식 | 단위 | 설명 |
|-------------|------|------|------|
| 클래스명 | String | - | 부품 종류 (4개 클래스) |
| 3D 위치 (camTobj) | [x, y, z] | 미터(m) | 카메라 좌표계에서 부품 중심까지의 거리 |
| 3D 회전 | [roll, pitch, yaw] | 도(°) | 카메라 좌표계에서 부품의 자세 |
| 가시성 | Float | 0~1 | 부품이 보이는 비율 (1-occlusion) |

#### 2.1.3 좌표계 정의

본 시스템은 **OpenCV/Depth 좌표계**를 사용합니다:
- X축: 오른쪽 (+)
- Y축: 아래쪽 (+)
- Z축: 앞쪽 (+, forward)

#### 2.1.4 데이터 파일 구조

```
dataset_pos_depth/
├── dataset_info.json           # 전체 데이터셋 메타정보
├── arm_link_25_nomat/
│   ├── metadata.json           # 클래스별 메타정보
│   ├── rgb_0000.png            # RGB 이미지
│   ├── distance_to_camera_0000.npy  # Depth 맵
│   ├── pose_0000.json          # 6DoF Pose 라벨
│   └── ...
└── (기타 클래스 폴더)
```

### 2.2 AI 모델 구축을 위한 하이퍼파라미터 및 학습 데이터 전처리 기법 조사

#### 2.2.1 부품 분류 모델 (Classification)

**모델 아키텍처**: ResNet18 (Transfer Learning)

| 하이퍼파라미터 | 설정값 | 설명 |
|----------------|--------|------|
| Backbone | ResNet18 | ImageNet 사전학습 가중치 사용 |
| 입력 크기 | 224×224 | ResNet 표준 입력 크기 |
| 배치 크기 | 32 (자동 조정) | GPU 메모리에 따라 조정 |
| 학습률 | 0.001 | Adam 옵티마이저 |
| 에포크 | 30 | Early Stopping 적용 (patience=5) |
| Dropout | 0.5, 0.3 | FC 레이어에 적용 |
| 손실 함수 | CrossEntropyLoss | 클래스 가중치 적용 |

**모델 구조**:
```
ResNet18 Feature Extractor (512 features)
    ↓
Dropout(0.5)
    ↓
Linear(512 → 256) + ReLU
    ↓
Dropout(0.3)
    ↓
Linear(256 → 4 classes)
```

**학습 결과**:
| 지표 | 값 |
|------|-----|
| 전체 정확도 | **96.75%** |
| 평균 Precision | 96.85% |
| 평균 Recall | 96.75% |
| 평균 F1-Score | 96.75% |

**클래스별 정확도**:
| 클래스 | 정확도 |
|--------|--------|
| arm_link_25_nomat | 98.0% |
| arm_link_30_notmat | 91.0% |
| boom_link_25_nomat | 99.0% |
| boom_link_30_notmat | 99.0% |

#### 2.2.2 위치 추정 모델 (6DoF Pose Estimation)

**모델 아키텍처**: DenseFusion 방식 (RGB + Depth 융합)

| 하이퍼파라미터 | 설정값 | 설명 |
|----------------|--------|------|
| RGB Encoder | ResNet50 | 이미지 특징 추출 |
| Depth Encoder | Custom CNN | Depth 특징 추출 |
| 융합 방식 | Concatenation | RGB + Depth 특징 결합 |
| 배치 크기 | 8~16 | GPU 메모리 제약 |
| 학습률 | 5e-5 ~ 1e-4 | Adam 옵티마이저 |
| 에포크 | 100~150 | |
| 손실 함수 | Smooth L1 Loss | 회귀 학습에 안정적 |

**위치 출력**:
- 3D Translation: (x, y, z) 미터 단위
- 3D Rotation: 6D Rotation Representation → Euler 변환

**학습 결과**:

<!-- 실험 결과를 아래 표에 채워주세요 -->
| 모델 | Mean Position Error | X 오차 | Y 오차 | Z 오차 |
|------|---------------------|--------|--------|--------|
| XY 회귀 (작업대 기준) | ___ mm | ___ mm | ___ mm | - |
| 6DoF (RGB only) | ___ mm | ___ mm | ___ mm | ___ mm |
| 6DoF (RGB+Depth) | ___ mm | ___ mm | ___ mm | ___ mm |
| Depth GT 기반 | ___ mm | ___ mm | ___ mm | ___ mm |

> **목표 정밀도**: 10mm (1cm) 이내

#### 2.2.3 데이터 전처리 기법

| 전처리 단계 | 방법 | 파라미터 |
|-------------|------|----------|
| 이미지 리사이즈 | Resize | 1024×1024 → 224×224 |
| 정규화 | ImageNet 통계 | mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225] |
| BBox 크롭 | 부품 영역 추출 | 배경 제거 효과 |
| Depth 정규화 | Min-Max | 0.1m ~ 10m → 0~1 |

### 2.3 데이터 증강 기법 및 모델 경량화 기술 조사

#### 2.3.1 데이터 증강 기법

**1) 합성 데이터 생성 시 Domain Randomization**:

| 랜덤화 항목 | 범위 | 목적 |
|-------------|------|------|
| 카메라 위치 | 부품 중심 기준 사선 구도 | 다양한 시점 확보 |
| 바닥 색상 | RGB (0.2, 0.2, 0.2) ~ (0.6, 0.5, 0.4) | 공장 환경 시뮬레이션 |
| 벽 색상 | RGB (0.4, 0.4, 0.4) ~ (0.9, 0.9, 0.85) | 배경 다양성 |
| 조명 위치 | 부품 중심 기준 랜덤 배치 | 다양한 그림자 패턴 |

**2) 학습 시 실시간 증강**:

```python
train_transform = transforms.Compose([
    transforms.Resize((224, 224)),
    transforms.RandomHorizontalFlip(p=0.5),
    transforms.RandomRotation(degrees=15),
    transforms.ColorJitter(brightness=0.2, contrast=0.2, saturation=0.2),
    transforms.ToTensor(),
    transforms.Normalize(mean=[0.485, 0.456, 0.406],
                        std=[0.229, 0.224, 0.225])
])
```

| 증강 기법 | 파라미터 | 적용 대상 |
|-----------|----------|-----------|
| RandomHorizontalFlip | p=0.5 | 분류 모델 |
| RandomRotation | 15° | 분류 모델 |
| ColorJitter | brightness=0.2, contrast=0.2 | 분류/위치 |
| Gaussian Noise (Depth) | σ=0.02 | 위치 추정 |

#### 2.3.2 모델 경량화 기술

| 기술 | 적용 여부 | 설명 |
|------|-----------|------|
| Transfer Learning | ✓ | ImageNet 사전학습 가중치 활용으로 학습 시간 단축 |
| ResNet18 사용 | ✓ | ResNet50 대비 파라미터 수 감소 (11M vs 25M) |
| Dropout | ✓ | 과적합 방지 및 모델 일반화 향상 |
| 배치 크기 자동 조정 | ✓ | GPU 메모리에 따른 최적화 |
| Early Stopping | ✓ | 불필요한 학습 방지 (patience=5) |

---

## 3. 학습 데이터 수집을 위한 하드웨어 장치 선정

### 3.1 부품 인식을 위한 카메라 및 센서 종류 분석

본 연구에서는 **합성 데이터 기반** 접근 방식을 채택하여, NVIDIA Isaac Sim 시뮬레이터를 통해 가상 환경에서 데이터를 생성하였습니다.

#### 3.1.1 시뮬레이션 카메라 설정

| 항목 | 설정값 | 설명 |
|------|--------|------|
| 해상도 | 1024×1024 | 고해상도 RGB + Depth |
| FOV | 60° | 표준 시야각 |
| Focal Length (fx, fy) | 768.0, 768.0 | 내재 파라미터 |
| Principal Point (cx, cy) | 512.0, 512.0 | 이미지 중심 |
| Depth 범위 | 0.1m ~ 10m | 유효 측정 범위 |

#### 3.1.2 필요 센서 종류

| 센서 유형 | 용도 | 출력 데이터 |
|-----------|------|-------------|
| RGB 카메라 | 부품 외관 촬영 | PNG 이미지 |
| Depth 센서 | 거리 정보 수집 | NPY 파일 (float32) |

### 3.2 주요 제품 스펙 비교를 통한 최적 장비 선정

#### 3.2.1 향후 실제 장비 적용 시 권장 사양

| 센서 유형 | 권장 사양 | 비고 |
|-----------|-----------|------|
| RGB 카메라 | 1024×1024 이상, 60fps | 산업용 카메라 |
| Depth 센서 | 0.1m ~ 10m 범위, mm 정밀도 | Intel RealSense, Azure Kinect 등 |
| 조명 | 일정한 조도 유지 | LED 조명 권장 |
| 마운트 | 고정식 또는 로봇 암 장착 | 사선 각도 촬영 가능 |

#### 3.2.2 권장 Depth 센서 비교

| 제품 | 해상도 | 범위 | 정밀도 | 비고 |
|------|--------|------|--------|------|
| Intel RealSense D435 | 1280×720 | 0.3~3m | ±2mm | 근거리 적합 |
| Intel RealSense L515 | 1024×768 | 0.25~9m | ±5mm | 중거리 적합 |
| Azure Kinect DK | 1024×1024 | 0.25~5.46m | ±11mm | 넓은 FOV |

---

## 4. 학습 데이터 수집을 위한 서버 및 DBMS 환경 구축

### 4.1 카메라/센서 연동을 위한 신호처리 소프트웨어 설계

#### 4.1.1 데이터 생성 파이프라인 (Isaac Sim 기반)

```
┌─────────────────────────────────────────────────────────────┐
│                    NVIDIA Isaac Sim                          │
├─────────────────────────────────────────────────────────────┤
│  [USD 부품 모델 로드]                                        │
│         ↓                                                    │
│  [Replicator 설정]                                           │
│    - 카메라 생성 (위치/FOV 설정)                              │
│    - 조명 설정 (Dome Light + Point Light)                    │
│    - Domain Randomization 적용                               │
│         ↓                                                    │
│  [프레임별 데이터 생성 (Step Loop)]                           │
│    - 카메라 위치 랜덤화                                       │
│    - 배경/조명 색상 랜덤화                                    │
│    - 렌더링 실행                                             │
│         ↓                                                    │
│  [Writer를 통한 파일 저장]                                    │
│    - RGB: rgb_####.png                                       │
│    - Depth: distance_to_camera_####.npy                      │
│    - BBox: bounding_box_2d_tight_####.npy                    │
│    - Pose: pose_####.json                                    │
└─────────────────────────────────────────────────────────────┘
```

#### 4.1.2 주요 소프트웨어 구성

| 구성 요소 | 버전/설명 | 용도 |
|-----------|-----------|------|
| NVIDIA Isaac Sim | 2023.1+ | 시뮬레이션 환경 |
| omni.replicator | 내장 | 데이터 생성 API |
| PyTorch | ≥2.0.0 | 딥러닝 프레임워크 |
| torchvision | ≥0.15.0 | 이미지 처리/모델 |
| NumPy | ≥1.21.0 | 수치 계산 |
| scikit-learn | ≥1.0.0 | Train/Test 분할 |
| Pillow | ≥9.0.0 | 이미지 처리 |

### 4.2 수집된 신호 데이터의 효율적 저장/관리를 위한 DBMS 스키마 설계

#### 4.2.1 파일 기반 데이터 저장 구조

현재 시스템은 **파일 시스템 기반**으로 데이터를 저장하며, JSON 형식의 메타데이터를 활용합니다.

**전체 데이터셋 메타정보 (dataset_info.json)**:
```json
{
  "name": "Excavator Parts 6DoF Pose Dataset (RGB + Depth)",
  "num_classes": 4,
  "images_per_class": 2000,
  "classes": [
    "arm_link_25_nomat",
    "arm_link_30_notmat",
    "boom_link_25_nomat",
    "boom_link_30_notmat"
  ],
  "depth_enabled": true,
  "resolution": [1024, 1024],
  "created_at": "2025-12-30 11:24:03"
}
```

**개별 프레임 라벨 (pose_####.json)**:
```json
{
  "class_name": "arm_link_25_nomat",
  "frame_index": 0,
  "raw_pose_world": {
    "t_xyz_m": [x, y, z],
    "r_xyz_deg": [0.0, 0.0, 0.0]
  },
  "camera_pose_world": {
    "t_xyz_m": [x, y, z],
    "r_xyz_deg": [roll, pitch, yaw]
  },
  "camTobj": {
    "t_xyz_m": [x, y, z],
    "r_xyz_deg": [roll, pitch, yaw]
  },
  "bbox_2d": {
    "x_min": 100, "y_min": 150,
    "x_max": 800, "y_max": 900,
    "visibility": 0.95
  },
  "image_info": {
    "resolution": [1024, 1024],
    "rgb_file": "rgb_0000.png",
    "depth_file": "distance_to_camera_0000.npy"
  }
}
```

#### 4.2.2 데이터 관리 스키마

| 테이블/폴더 | 필드/파일 | 설명 |
|-------------|-----------|------|
| dataset_info | name, num_classes, images_per_class | 전체 데이터셋 정보 |
| {class_name}/ | metadata.json | 클래스별 메타정보 |
| {class_name}/ | rgb_####.png | RGB 이미지 |
| {class_name}/ | distance_to_camera_####.npy | Depth 맵 |
| {class_name}/ | pose_####.json | 6DoF Pose 라벨 |
| {class_name}/ | bounding_box_2d_tight_####.npy | 2D BBox |

---

## 5. 부품 인식 AI 학습용 기초 데이터셋 구축

### 5.1 부품 인식 학습을 위한 모사 시험 환경 구축

#### 5.1.1 NVIDIA Isaac Sim 시뮬레이션 환경

<!-- 
[그림 삽입 필요: Isaac Sim 시뮬레이션 환경 스크린샷]

다음 프롬프트로 AI 이미지 생성 또는 Isaac Sim 캡처:
"NVIDIA Isaac Sim에서 굴착기 부품(arm link, boom link)이 
공장 바닥 위에 배치된 시뮬레이션 환경. 
카메라가 사선 각도로 부품을 촬영하는 구도.
조명은 Dome Light와 Point Light 사용."
-->

| 환경 구성 요소 | 설정 | 설명 |
|----------------|------|------|
| 부품 모델 | USD 형식 (4종) | 굴착기 arm/boom link |
| 바닥 평면 | 부품 크기의 5배 | 공장 바닥 시뮬레이션 |
| 뒷벽 평면 | 바닥의 절반 크기 | 배경 시뮬레이션 |
| Dome Light | 800 intensity | 환경광 |
| Point Light ×2 | 50000, 30000 intensity | 주/보조 조명 |
| 카메라 | 60° FOV, 1024×1024 | 사선 구도 촬영 |

#### 5.1.2 부품 모델 정보

| 부품명 | USD 파일 | 크기 (약) | 특징 |
|--------|----------|-----------|------|
| arm_link_25_nomat | arm_link_25_nomat.usd | ~4m | 25톤급 암 링크 |
| arm_link_30_notmat | arm_link_30_notmat.usd | ~4m | 30톤급 암 링크 |
| boom_link_25_nomat | boom_link_25_nomat.usd | ~4m | 25톤급 붐 링크 |
| boom_link_30_notmat | boom_link_30_notmat.usd | ~4m | 30톤급 붐 링크 |

### 5.2 다양한 조건을 반영한 다중 형태 데이터 수집 및 전처리

#### 5.2.1 생성된 데이터셋 통계

**분류용 데이터셋** (`class_estimation/datasets/`):

| 클래스 | 이미지 수 | 형식 |
|--------|-----------|------|
| arm_link_25_nomat | 500장 | RGB + BBox |
| arm_link_30_notmat | 500장 | RGB + BBox |
| boom_link_25_nomat | 500장 | RGB + BBox |
| boom_link_30_notmat | 500장 | RGB + BBox |
| **합계** | **2,000장** | |

**위치추정용 데이터셋** (`pos_estimation/dataset_pos_depth/`):

| 클래스 | 이미지 수 | 형식 |
|--------|-----------|------|
| arm_link_25_nomat | 2,000장 | RGB + Depth + Pose |
| arm_link_30_notmat | 2,000장 | RGB + Depth + Pose |
| boom_link_25_nomat | 2,000장 | RGB + Depth + Pose |
| boom_link_30_notmat | 2,000장 | RGB + Depth + Pose |
| **합계** | **8,000장** | |

#### 5.2.2 데이터 다양성 확보 방법

| 다양성 요소 | 적용 방법 | 범위 |
|-------------|-----------|------|
| 카메라 시점 | 사선 구도 랜덤화 | X: 0.3~0.7, Y: 0.1~0.5, Z: 0.2~0.4 (비율) |
| 배경 색상 | 바닥/벽 색상 랜덤화 | 회색~갈색/베이지 계열 |
| 조명 조건 | 조명 위치 랜덤화 | 부품 중심 기준 다양한 위치 |
| 그림자 | 조명 위치에 따른 자연스러운 그림자 | 자동 생성 |

### 5.3 데이터 증강(Data Augmentation) 기법 적용을 통한 학습용 데이터 다양성 확보

#### 5.3.1 합성 데이터 생성 시 증강 (Domain Randomization)

Domain Randomization은 **Sim-to-Real 전이**를 개선하기 위해 시뮬레이션에서 다양한 조건을 랜덤하게 적용하는 기법입니다.

```python
# 카메라 위치 랜덤화 코드 예시
with rep.create.group([camera]):
    rep.modify.pose(
        position=rep.distribution.uniform(
            (part_center[0] + camera_distance_max * 0.3,
             part_center[1] + camera_distance_max * 0.1,
             part_center[2] + camera_distance_max * 0.2),
            (part_center[0] + camera_distance_max * 0.7,
             part_center[1] + camera_distance_max * 0.5,
             part_center[2] + camera_distance_max * 0.4)
        ),
        look_at=part_center
    )

# 배경 색상 랜덤화 코드 예시
with floor_plane:
    rep.randomizer.color(
        colors=rep.distribution.uniform(
            (0.2, 0.2, 0.2),  # 어두운 회색
            (0.6, 0.5, 0.4)   # 밝은 갈색
        )
    )
```

#### 5.3.2 학습 시 실시간 증강

| 증강 기법 | 적용 모델 | 파라미터 | 효과 |
|-----------|-----------|----------|------|
| RandomHorizontalFlip | 분류 | p=0.5 | 좌우 대칭 학습 |
| RandomRotation | 분류 | 15° | 회전 불변성 |
| ColorJitter | 분류/위치 | brightness/contrast=0.2 | 조명 변화 대응 |
| Gaussian Noise | 위치 (Depth) | σ=0.02 | 센서 노이즈 대응 |

#### 5.3.3 증강 효과

| 지표 | 증강 미적용 | 증강 적용 | 개선율 |
|------|-------------|-----------|--------|
| 분류 정확도 | ~90% | **96.75%** | +6.75%p |
| 과적합 감소 | 높음 | 낮음 | 개선됨 |
| 일반화 성능 | 낮음 | 높음 | 개선됨 |

---

## 부록: 산출물 목록

### A. 코드 산출물

| 파일 | 경로 | 설명 |
|------|------|------|
| 데이터셋 생성 (분류) | `class_estimation/01_generate_multi_class_dataset.py` | Isaac Sim 기반 |
| 분류 학습 | `class_estimation/02_parts_classification.py` | ResNet18 |
| 분류 평가 | `class_estimation/03_parts_class_evaluation.py` | 정확도 측정 |
| 데이터셋 생성 (위치) | `pos_estimation/01_generate_mult_class_dataset_with_pos.py` | RGB+Depth+Pose |
| XY 위치 추정 | `pos_estimation/04_parts_pos_estimation.py` | 2D 회귀 |
| ICP 위치 추정 | `pos_estimation/05_icp_pose_estimation.py` | Point Cloud 기반 |
| 6DoF 추정 | `pos_estimation/06_deep_6dof_estimation.py` | DenseFusion 방식 |
| Depth 기반 추정 | `pos_estimation/07_depth_based_pose.py` | Depth GT 활용 |

### B. 모델 산출물

| 파일 | 경로 | 설명 |
|------|------|------|
| 분류 모델 | `class_estimation/artifacts/best_parts_model.pth` | 96.75% 정확도 |
| 클래스 목록 | `class_estimation/artifacts/class_names.json` | 4개 클래스 |
| 학습 정보 | `class_estimation/artifacts/training_indices_parts.json` | Train/Test 분할 |
| 평가 결과 | `class_estimation/evaluation_results_parts.json` | 성능 지표 |
| 위치 추정 모델 | `pos_estimation/artifacts/best_parts_xy_regressor.pth` | XY 회귀 |
| 정규화 통계 | `pos_estimation/artifacts/xy_normalization_stats.json` | Mean/Std |

### C. 데이터셋 산출물

| 데이터셋 | 경로 | 크기 |
|----------|------|------|
| 분류용 | `class_estimation/datasets/` | 2,000장 (4 클래스 × 500장) |
| 위치추정용 | `pos_estimation/dataset_pos_depth/` | 8,000장 (4 클래스 × 2,000장) |

---

**문서 작성자**: AI Assistant  
**최종 수정일**: 2025년 12월 30일

