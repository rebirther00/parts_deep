# ==========================================
# pos_estimation 프로젝트 의존성 패키지
# ==========================================
# 설치: pip install -r requirements.txt
#
# 참고: Isaac Sim 관련 패키지(isaacsim, omni, pxr)는 
#       Isaac Sim 설치 시 자동으로 포함됩니다.
# ==========================================

# PyTorch (CUDA 버전 권장)
# GPU 사용 시: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch>=2.0.0
torchvision>=0.15.0

# 데이터 과학 / 머신러닝
numpy>=1.24.0
scikit-learn>=1.3.0

# 이미지 처리
Pillow>=10.0.0

# 3D Point Cloud 처리 (ICP Pose Estimation용)
# ⚠️ open3d는 aarch64(ARM64)에서 공식 미지원
# x86_64 시스템에서만 설치: pip install open3d>=0.18.0
# ARM64에서 ICP 기능 사용 시 소스 빌드 필요
# open3d>=0.18.0  # 선택적 - ARM64 미지원

# 진행 표시바
tqdm>=4.65.0

# ==========================================
# 선택적 패키지 (개발/디버깅용)
# ==========================================
# matplotlib>=3.7.0      # 시각화
# scipy>=1.11.0          # 고급 수학 연산

