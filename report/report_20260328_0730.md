# [연구 목표 및 내용] - 2차년도

**프로젝트명**: 굴착기 제조 공정 생산성 향상을 위한 AI 기반 부품 인식 및 위치 추정 시스템  
**수행기관**: 한국건설기계연구원  
**작성일**: 2026년 3월 28일  
**보고 구분**: 2차년도 연차보고서

---

## 목차

1. [굴착기 제조 공정 생산성 향상을 위한 공정 프로세스 기술 조사](#1-굴착기-제조-공정-생산성-향상을-위한-공정-프로세스-기술-조사)
2. [부품 인식 및 위치 추정 데이터 항목 정의 및 AI 모델 구축을 위한 기술조사](#2-부품-인식-및-위치-추정-데이터-항목-정의-및-ai-모델-구축을-위한-기술조사)
3. [학습 데이터 수집을 위한 하드웨어 장치 선정](#3-학습-데이터-수집을-위한-하드웨어-장치-선정)
4. [학습 데이터 수집을 위한 서버 및 DBMS 환경 구축](#4-학습-데이터-수집을-위한-서버-및-dbms-환경-구축)
5. [부품 인식 AI 학습용 기초 데이터셋 구축](#5-부품-인식-ai-학습용-기초-데이터셋-구축)
6. [부록: 산출물 목록](#6-부록-산출물-목록)
7. [추후 할일 (TODO)](#7-추후-할일-todo)

---

## 1. 굴착기 제조 공정 생산성 향상을 위한 공정 프로세스 기술 조사

### 1.1 굴착기 제조 공정의 현장 환경 및 흐름 조사

> ※ 본 항목은 현장 조사 기반으로 작성 필요

<!-- TODO: 현장 조사 결과 내용 기입 -->

### 1.2 주요 부품의 제작 공정 프로세스 기술 분석

> ※ 본 항목은 현장 조사 기반으로 작성 필요

<!-- TODO: 공정 프로세스 분석 내용 기입 -->

---

## 2. 부품 인식 및 위치 추정 데이터 항목 정의 및 AI 모델 구축을 위한 기술조사

### 2.1 공정 흐름 기반의 부품 인식 및 위치 추정을 위한 데이터 항목 정의

#### 2.1.1 연구 배경 및 목적

굴착기 제조 공정에서 부품의 자동 인식 및 위치 추정은 생산성 향상의 핵심 기술이다. 이를 위해서는 카메라·센서로부터 획득되는 영상 데이터와 AI 모델이 요구하는 입·출력 데이터의 형식, 구조, 단위 등을 명확히 정의하는 것이 선행되어야 한다.

본 연구에서는 1차년도에서 수립한 RGB 기반 부품 분류 데이터 체계를 확장하여, **RGBD 4채널 (RGB + Depth)** 기반의 데이터 항목을 새롭게 정의하였다. Depth 채널을 추가함으로써 부품의 외관 정보뿐만 아니라 3차원 형상 정보를 동시에 활용할 수 있게 되어, 형상이 유사한 부품 간의 분류 정확도를 향상시킬 수 있다.

#### 2.1.2 대상 부품 및 클래스 체계

2차년도에서는 굴착기 도어(Door) 부품을 대상으로 데이터 항목을 정의하였다. 굴착기 3개 모델(E25, E30, E38)의 도어 3종(door_RH, door_LH_FRT, door_LH_RR)을 조합하여 총 **8개 클래스**를 구성하였다. E30과 E38의 door_RH는 동일 부품이므로 `E30_E38_door_RH`로 통합하였다.

| 번호 | 클래스 식별자 | 굴착기 모델 | 부품 유형 | 크기(mm) | 비고 |
|------|-------------|-----------|----------|---------|------|
| 0 | E25_door_LH_FRT | E25 | 좌측 전방 도어 | 828×1140 | |
| 1 | E25_door_LH_RR | E25 | 좌측 후방 도어 | 1141×1140 | |
| 2 | E25_door_RH | E25 | 우측 도어 | 990×1140 | |
| 3 | E30_E38_door_RH | E30/E38 | 우측 도어 | 1191×1140 | E30/E38 동일 부품 통합 |
| 4 | E30_door_LH_FRT | E30 | 좌측 전방 도어 | 869×1140 | E25 대비 +41mm |
| 5 | E30_door_LH_RR | E30 | 좌측 후방 도어 | 1262×1140 | |
| 6 | E38_door_LH_FRT | E38 | 좌측 전방 도어 | 916×1140 | E30 대비 +47mm |
| 7 | E38_door_LH_RR | E38 | 좌측 후방 도어 | 1456×1140 | |

> **표 2-1.** 굴착기 도어 부품 8클래스 체계

#### 2.1.3 부품 분류용 데이터 항목 정의

부품 분류 모델의 입·출력에 사용되는 데이터 항목을 다음과 같이 정의하였다.

**[입력 데이터 항목]**

| 데이터 항목 | 형식 | 크기 | 단위 | 설명 |
|------------|------|------|------|------|
| RGB 이미지 | 8-bit PNG | 1920×1080 | 픽셀 | ZED X Mini 카메라 촬영 컬러 이미지 |
| Depth 이미지 | 16-bit PNG | 1920×1080 | 밀리미터(mm) | Neural Depth 모드 깊이 맵 |
| RGBD 4채널 텐서 | Float32 Tensor | [B, 4, 448, 448] | 정규화 | 모델 입력용 RGB+Depth 결합 텐서 |
| 물리 치수 보조 피처 | Float32 Tensor | [B, 3] | mm/무차원 | SAM 마스크 + 3D PCA 기반 [width, height, aspect] |

> **표 2-2.** 부품 분류 입력 데이터 항목

**[출력 데이터 항목]**

| 데이터 항목 | 형식 | 단위 | 설명 |
|------------|------|------|------|
| 클래스 레이블 | Integer (0~7) | - | 8개 클래스 중 하나 |
| 분류 확률 | Float32 [8] | 0.0~1.0 | Softmax 확률 분포 |
| 예측 클래스명 | String | - | 클래스 식별자 문자열 |

> **표 2-3.** 부품 분류 출력 데이터 항목

#### 2.1.4 위치 추정용 데이터 항목 정의

부품의 3차원 위치 및 자세를 추정하기 위한 6DoF(6 Degrees of Freedom) 포즈 데이터 항목을 정의하였다.

| 데이터 항목 | 형식 | 단위 | 좌표계 | 설명 |
|------------|------|------|--------|------|
| 3D 위치 (Translation) | [x, y, z] | 미터(m) | 카메라 기준 | 카메라-부품 간 상대 위치 |
| 3D 자세 (Rotation) | [qx, qy, qz, qw] | 쿼터니언 | 카메라 기준 | 부품의 3D 회전 자세 |
| 2D 바운딩 박스 | [x_min, y_min, x_max, y_max] | 픽셀 | 이미지 좌표 | 부품 영역 좌표 |
| 카메라 내부 파라미터 | K 행렬 [3×3] | 픽셀 | - | fx, fy, cx, cy |
| 가림 비율 | Float | 0.0~1.0 | - | 0.0=완전 보임, 1.0=완전 가림 |

> **표 2-4.** 위치 추정 데이터 항목

**[좌표계 규약]**

본 시스템은 카메라 광학(optical) 좌표계를 기준으로 한다.
- X축: 오른쪽 (+)
- Y축: 아래쪽 (+)
- Z축: 전방 (+, 카메라가 바라보는 방향)

#### 2.1.5 메타데이터 관리 체계

데이터셋의 체계적 관리를 위해 2단계 계층적 JSON 메타데이터 구조를 설계하였다.

**[전체 데이터셋 메타데이터 (dataset_info.json)]**

```json
{
  "dataset_name": "Excavator Door Classification Dataset",
  "num_classes": 8,
  "total_images": 5256,
  "classes": {
    "E25_door_LH_FRT": "E25_door_LH_FRT",
    "E30_door_LH_FRT": "E30_door_LH_FRT",
    "E38_door_LH_FRT": "E38_door_LH_FRT"
  },
  "data_source": "real_camera",
  "camera": "ZED X Mini",
  "depth_enabled": true,
  "created_at": "2026-03-27"
}
```

**[클래스별 메타데이터 (metadata.json)]**

```json
{
  "class_name": "E25_door_LH_FRT",
  "display_name": "E25 Door LH Front",
  "data_source": "real_camera",
  "camera": "ZED X Mini",
  "resolution": [1920, 1080],
  "num_images": 110,
  "capture_method": "video_frame_extraction",
  "frame_extraction_interval": 5,
  "quality_filter": true
}
```

#### 2.1.6 데이터셋 디렉토리 구조

데이터셋은 분류용과 6DoF 포즈용의 두 가지 유형으로 구분하여 관리하며, 각각의 목적에 맞는 어노테이션 형식을 설계하였다.

```
class_estimation/door/
├── datasets/                          # 실물 촬영 RGBD 데이터
│   ├── dataset_info.json              # 전체 메타데이터
│   ├── E25_door_LH_FRT/
│   │   ├── metadata.json              # 클래스 메타데이터
│   │   ├── rgb_0000.png               # RGB 이미지 (8-bit)
│   │   ├── depth_0000.png             # Depth 이미지 (16-bit, mm)
│   │   ├── mask_0000.png              # SAM2 전경 마스크
│   │   └── ...
│   ├── E30_door_LH_FRT/
│   └── E38_door_LH_FRT/
│
├── datasets_cad/                      # CAD 합성 RGBD 데이터
│   ├── dataset_info.json
│   ├── E25_door_LH_FRT/
│   │   ├── rgb_0000.png               # 합성 RGB (8-bit)
│   │   ├── depth_0000.png             # 합성 Depth (16-bit, mm)
│   │   ├── mask_0000.png              # SAM2 전경 마스크
│   │   └── ...  (각 500장)
│   ├── E25_door_LH_RR/
│   ├── E25_door_RH/
│   ├── E30_door_LH_FRT/
│   ├── E30_door_LH_RR/
│   ├── E30_E38_door_RH/
│   ├── E38_door_LH_FRT/
│   └── E38_door_LH_RR/
│
├── sam_models/                        # SAM 세그멘테이션 모델
│   ├── mobile_sam.pt                  # MobileSAM (39MB)
│   └── sam2.1_hiera_small.pt          # SAM2.1 Small (176MB)
│
├── deprecated/                        # 폐기된 코드
│   ├── depth_utils_bbox_aux.py        # 기존 BBox 방식
│   ├── depth_utils_otsu_pca.py        # Otsu+PCA 방식 (SAM 이전)
│   ├── 04_door_realtime_inference_no_sam.py
│   └── 02_door_cad_classification_5090_2step.py
│
└── artifacts/                         # 학습 산출물
    ├── best_door_model_5090.pth       # PyTorch 모델 가중치
    ├── best_door_model_5090.onnx      # ONNX 변환 모델
    ├── class_names_door_5090.json     # 클래스 이름 목록
    └── evaluation_results_door_5090.* # 평가 결과
```

> **그림 2-1.** 데이터셋 디렉토리 구조

---

### 2.2 AI 모델 구축을 위한 하이퍼파라미터 및 학습 데이터 전처리 기법 조사

#### 2.2.1 모델 아키텍처 선정

부품 분류를 위한 기반 모델로 **ResNet18**을 선정하였다. ResNet18은 18개 레이어로 구성된 잔차 학습(Residual Learning) 기반의 합성곱 신경망으로, 약 1,100만 개의 파라미터를 가진 경량 모델이다. ImageNet 데이터셋(1,000개 클래스, 128만 장)으로 사전학습된 가중치를 활용한 **전이학습(Transfer Learning)** 방식을 채택하여, 소규모 데이터셋(수백 장 수준)에서도 안정적인 학습 수렴과 높은 분류 정확도를 확보하였다.

**[RGBD 4채널 입력 확장]**

기존 ResNet18은 RGB 3채널 입력을 전제로 설계되어 있으므로, RGBD 4채널 입력을 지원하기 위해 첫 번째 합성곱 레이어(conv1)를 수정하였다. 구체적으로, 3채널 → 4채널로 확장된 새로운 Conv2d(4, 64, kernel_size=7, stride=2, padding=3) 레이어를 생성하고, 기존 ImageNet 사전학습 가중치의 RGB 3채널분은 그대로 복사하며, 추가된 Depth 채널(4번째)은 기존 RGB 3채널 가중치의 **평균값**으로 초기화하였다. 이를 통해 Depth 채널이 초기에 RGB 특징의 평균적 반응을 보이면서 안정적으로 학습을 시작할 수 있다.

```
RGBDAuxResNet18 모델 구조
═══════════════════════════════════════════════════════

입력 1: [Batch, 4, 448, 448]  (R, G, B, D)     입력 2: [Batch, 3]  (물리 치수)
      │                                              │
      ▼                                              ▼
┌─────────────────────────────────────────┐  ┌────────────────────────┐
│  Conv2d(4→64, 7×7, stride=2, pad=3)    │  │  Aux MLP               │
│  BatchNorm2d(64)                        │  │  Linear(3→32) + ReLU   │
│  ReLU                                   │  │  → [Batch, 32]        │
│  MaxPool2d(3×3, stride=2)               │  └───────────┬────────────┘
├─────────────────────────────────────────┤              │
│  ResNet18 Residual Blocks               │              │
│  ├── Layer1: BasicBlock × 2 (64→64)     │              │
│  ├── Layer2: BasicBlock × 2 (64→128)    │              │
│  ├── Layer3: BasicBlock × 2 (128→256)   │              │
│  └── Layer4: BasicBlock × 2 (256→512)   │              │
├─────────────────────────────────────────┤              │
│  AdaptiveAvgPool2d → [Batch, 512]       │              │
└────────────────────┬────────────────────┘              │
                     │          ┌─────────────────────────┘
                     └──────────┤
                                ▼
                     Concatenate [512 + 32 = 544]
                                │
                                ▼
                     ┌───────────────────────┐
                     │  Dropout(0.3)         │
                     │  Linear(544→256)+ReLU │
                     │  Dropout(0.2)         │
                     │  Linear(256→N_class)  │
                     └───────────────────────┘
                                │
                                ▼
                     출력: [Batch, N_class] (로짓)
```

> **그림 2-2.** RGBDAuxResNet18 모델 아키텍처 (CNN 특징 + 물리 치수 보조 피처 결합)

#### 2.2.2 하이퍼파라미터 설정

모델 학습에 사용된 하이퍼파라미터는 사전 실험을 통해 최적값을 탐색하여 결정하였다.

| 하이퍼파라미터 | 설정값 | 선정 근거 |
|--------------|--------|---------|
| 옵티마이저 | Adam (lr=0.001) | 전체 파라미터 단일 학습률 적용 |
| 학습률 스케줄러 | ReduceLROnPlateau | 검증 손실 정체 시 자동 학습률 감소 (patience=3, factor=0.5) |
| 배치 크기 | 64 | RTX 5090 GPU(32GB VRAM) 메모리 기준 최적화, 자동 조정 지원 |
| 최대 에포크 | 60 | 과적합 방지를 위한 충분한 학습 기회 부여 |
| Early Stopping | patience=10 | 검증 정확도 10 에포크 연속 미개선 시 조기 종료 |
| 손실 함수 | CrossEntropyLoss | 다중 클래스 분류의 표준 손실 함수, 클래스 가중치 적용 |
| 클래스 가중치 | 역빈도 기반 | 클래스 불균형 보정 (이미지 수 적은 클래스에 높은 가중치) |
| 데이터 분할 | Train 80% / Test 20% | Stratified Split으로 클래스 비율 유지 (Train 1,004장, Test 252장) |
| Dropout | 0.3 / 0.2 | FC 레이어 과적합 방지 |

> **표 2-5.** 모델 학습 하이퍼파라미터 설정

**[학습 결과 (실물 8종 분류)]**

| 지표 | 값 |
|------|-----|
| 학습 데이터 | 1,256장 (8클래스, Train 1,004장 / Test 252장) |
| 학습 에포크 | 30 에포크에서 최고 정확도 달성 (40 에포크에서 Early Stopping) |
| 학습 시간 | 약 84분 (RTX 5090) |
| 최고 검증 정확도 | **100.00%** |
| 테스트 정확도 | **100.00%** (252/252장) |

> **표 2-6.** 실물 데이터 8종 분류 학습 결과

| 클래스 | 테스트 샘플 수 | 정확도 | Precision | Recall | F1-Score |
|--------|-------------|--------|-----------|--------|----------|
| E25_door_LH_FRT | 32 | 100.00% | 100.00% | 100.00% | 100.00% |
| E25_door_LH_RR | 31 | 100.00% | 100.00% | 100.00% | 100.00% |
| E25_door_RH | 31 | 100.00% | 100.00% | 100.00% | 100.00% |
| E30_E38_door_RH | 31 | 100.00% | 100.00% | 100.00% | 100.00% |
| E30_door_LH_FRT | 32 | 100.00% | 100.00% | 100.00% | 100.00% |
| E30_door_LH_RR | 32 | 100.00% | 100.00% | 100.00% | 100.00% |
| E38_door_LH_FRT | 31 | 100.00% | 100.00% | 100.00% | 100.00% |
| E38_door_LH_RR | 32 | 100.00% | 100.00% | 100.00% | 100.00% |
| **Macro Average** | **252** | **100.00%** | **100.00%** | **100.00%** | **100.00%** |

> **표 2-7.** 클래스별 분류 성능 상세 (8종)

<!-- TODO: 학습 곡선 그래프 삽입 (Loss/Accuracy vs Epoch) -->
<!-- 파일: artifacts/training_curve_door_5090.png -->

<!-- TODO: 혼동 행렬(Confusion Matrix) 이미지 삽입 -->
<!-- 파일: artifacts/evaluation_results_door_5090.png -->

#### 2.2.3 학습 데이터 전처리 기법

수집된 원시 이미지 데이터를 모델에 입력하기 위한 전처리 파이프라인을 설계·구현하였다. 굴착기 부품(도어 등)은 좌·우 방향 및 형상의 **종횡비(aspect ratio)가 분류의 핵심 특징**이므로, 기존의 CenterCrop 방식 대신 **Letterbox 방식**을 채택하였다. Letterbox 방식은 장변을 기준으로 이미지를 축소한 뒤, 단변 방향에 검정(0) 패딩을 추가하여 정사각형을 완성하므로, 원본 이미지의 종횡비가 완전히 보존되어 부품의 형상 정보가 손실 없이 모델에 전달된다. RGB와 Depth 데이터의 특성이 상이하므로, 각각에 적합한 전처리 기법을 적용하되 공간 변환은 동기화하여 기하학적 일관성을 유지하였다.

**[입력 해상도 448×448 선정 근거]**

초기에는 ImageNet 표준인 224×224 해상도를 사용하였으나, 유사 형상 부품(E25/E30/E38 도어) 간 크기 차이가 40~50mm에 불과한 점이 문제가 되었다. 1920×1080 원본에서 224×224로 축소 시 약 8.6배 다운샘플링되어, 50mm 물리 차이가 약 6픽셀 미만으로 줄어들어 모델이 클래스를 구분하지 못하는 현상이 발생하였다(34% 정확도). 이에 해상도를 **448×448**로 2배 증가시켜 유효 픽셀 차이를 약 12픽셀 이상으로 확보하였다. 메모리 사용량은 4배 증가하나, RTX 5090(32GB VRAM) 기준 배치 크기 16에서 안정적으로 학습 가능하다.

**[전처리 파이프라인]**

```
원본 RGB (1920×1080, 8-bit)          원본 Depth (1920×1080, 16-bit, mm)
        │                                      │
        ▼                                      ▼
  PIL.Image 로드 (RGB)                  cv2.imread (UNCHANGED)
        │                                      │
        ▼                                      ▼
  ┌─────────────────────── 동기화 공간 변환 ───────────────────────┐
  │  1) Letterbox Resize: 장변 기준 448px 축소 (종횡비 보존)       │
  │  2) 중앙 배치 패딩: 검정(0) 패딩으로 448×448 완성             │
  │  3) [학습 시] HorizontalFlip(50%)                              │
  │  4) [학습 시] RandomRotation(±5°)                             │
  │  5) [학습 시] RandomScale(0.9~1.1, 50%)                       │
  │     ※ RGB·Depth 동기화: 동일 파라미터 적용                     │
  │     ※ Depth 보간: INTER_NEAREST (거리값 왜곡 방지)             │
  └────────────────────────────────────────────────────────────────┘
        │                                      │
        ▼                                      ▼
  [학습 시] 색상·노이즈 증강             5m 클리핑 → [0, 1] 스케일링
  - Brightness (0.6~1.4)                       │
  - Contrast (0.6~1.4)                         ▼
  - Saturation (0.7~1.3)             정규화: (x - 0.5) / 0.25
  - Hue (±0.05)
  - GaussianNoise (30%, σ=3~10)
  - GaussianBlur (20%, k=3)
        │                                      │
        ▼                                      ▼
  ToTensor → Normalize               ToTensor (unsqueeze)
  mean=[0.485, 0.456, 0.406]                   │
  std=[0.229, 0.224, 0.225]                    │
        │                                      │
        └────────── torch.cat ─────────────────┘
                       │
                       ▼
              [Batch, 4, 448, 448]
              (R, G, B, D 채널)

        ┌─────── SAM + Depth 기반 물리 치수 계산 ──────┐
        │  1) SAM 마스크로 전경(부품) 영역 분리          │
        │     (학습: SAM2 오프라인, 추론: MobileSAM)     │
        │  2) 전경 픽셀 → 3D 점 변환 (핀홀 카메라)      │
        │  3) PCA → 시점 무관 물리 치수 (mm)            │
        │  → [width_mm, height_mm, aspect]             │
        │  ※ mean_depth 제거 (부품 종류와 무관)         │
        └──────────────────────────────────────────────┘
                       │
                       ▼
              [Batch, 3] 보조 피처 (Auxiliary Features)
```

> **그림 2-3.** RGBD 전처리 파이프라인

| 전처리 단계 | 적용 대상 | 방법 | 파라미터 | 비고 |
|------------|----------|------|---------|------|
| Letterbox Resize | RGB, Depth | 장변 기준 비례 축소 | 448px | 종횡비 완전 보존 |
| 중앙 배치 패딩 | RGB, Depth | 검정(0) 패딩으로 448×448 완성 | 448×448 | 동기화 적용 |
| ImageNet 정규화 | RGB | Channel-wise 표준화 | mean/std | ImageNet 통계값 |
| Depth 클리핑 | Depth | 최대 거리 제한 | 5,000mm (5m) | 원거리 노이즈 제거 |
| Depth 스케일링 | Depth | 선형 스케일링 | ÷5000 → [0, 1] | 값 범위 통일 |
| Depth 정규화 | Depth | Z-score 표준화 | (x-0.5)/0.25 | 분포 중심화 |
| 수평 반전 | RGB, Depth | 좌우 반전 (50% 확률) | - | 좌우 시점 다양성 확보 |
| 스케일 변환 | RGB, Depth | 0.9~1.1배 (50% 확률) | - | 카메라 거리 변화 시뮬레이션 |
| 색조 변환 | RGB | Hue ±0.05 | - | 색온도 변화 대응 |
| 가우시안 노이즈 | RGB | σ: 3~10 (30% 확률) | - | 센서 노이즈 대응 |
| 가우시안 블러 | RGB | kernel=3 (20% 확률) | - | 촬영 포커스 변화 대응 |
| 블러 필터링 | RGB | Laplacian Variance | threshold=100 | 흐릿한 이미지 제거 |

> **표 2-8.** 전처리 기법 상세

**[Depth 정규화 설계 근거]**

Depth 값은 물리적 거리(mm)를 나타내므로, RGB와는 다른 정규화 전략이 필요하다. 본 연구에서는 아래 3단계 정규화 방식을 채택하였다.

1. **클리핑**: 5m(5,000mm) 초과 값을 5m로 제한하여 원거리 노이즈 영향을 배제
2. **[0, 1] 스케일링**: 클리핑된 값을 5,000으로 나누어 0~1 범위로 정규화
3. **Z-score 표준화**: (x - 0.5) / 0.25 적용으로 평균 0, 표준편차 약 1의 분포로 변환

이 방식은 무효값(0, NaN, Inf)을 0.0으로 일관 처리하고, RGB 채널과 유사한 값 범위를 유지하여 합성곱 연산의 수치 안정성을 확보한다.

#### 2.2.4 SAM + 3D PCA 기반 물리 치수 보조 피처 (Auxiliary Features)

굴착기 도어 부품은 E25/E30/E38 모델 간 40~50mm의 미세한 크기 차이만 존재하여, 이미지 픽셀 정보만으로는 분류가 어렵다. 이를 해결하기 위해 **SAM(Segment Anything Model)** 기반 전경 분리와 **3D PCA(Principal Component Analysis)** 를 활용하여 **시점에 무관한 물리적 치수를 수치 피처로 직접 계산**하고, CNN 특징과 결합하는 방식을 도입하였다.

**[물리 치수 계산 원리 — 3단계]**

**1단계: SAM 기반 전경 분리**

SAM(Segment Anything Model)을 활용하여 RGB 이미지에서 전경(부품) 영역을 정밀하게 분리한다.

- **학습 데이터**: SAM2 (`facebook/sam2.1-hiera-small`)로 오프라인 마스크 생성 → `mask_NNNN.png`
- **실시간 추론**: MobileSAM (40.7MB, GPU ~10ms)으로 실시간 마스크 생성
- 이미지 중심점 프롬프트 → 최대 면적 마스크 선택
- **폴백**: SAM 마스크가 없는 경우, 기존 Depth Otsu 이진화 + 형태학적 처리 방식으로 대체

**2단계: 3D 점 변환 (핀홀 카메라 모델)**

전경 마스크 내의 픽셀을 Depth 값과 카메라 내부 파라미터를 이용하여 3D 공간 좌표로 변환한다.

```
3D 점 변환 공식:
  X = (pixel_x - cx) × depth / fx
  Y = (pixel_y - cy) × depth / fy
  Z = depth

  여기서 fx, fy = 초점거리, cx, cy = 주점(principal point)
```

**3단계: PCA로 시점 무관 치수 계산**

변환된 3D 점군에 SVD(특이값 분해)를 적용하여 주성분 2축을 추출하고, 각 축으로의 투영 범위를 물리 치수로 계산한다.

- SVD로 주성분 2축(PC1, PC2) 추출
- PC1, PC2 축 투영 범위 = 물리 width, height (mm)
- AR = max / min
- 카메라 시점이 달라져도 일관된 3D 치수를 반환

| 보조 피처 | 계산 방법 | 단위 | 의미 |
|----------|----------|------|------|
| physical_width_mm | 3D PCA PC1/PC2 중 큰 축 범위 | mm | 부품 긴 변 |
| physical_height_mm | 3D PCA PC1/PC2 중 작은 축 범위 | mm | 부품 짧은 변 |
| aspect_ratio | width / height | 무차원 | 종횡비 |

> **표 2-8b.** SAM + 3D PCA 기반 물리 치수 보조 피처 (NUM_AUX_FEATURES = 3)

> **mean_depth_mm 제거 완료**: mean_depth_mm(카메라~부품 평균 거리)은 도어 종류와 무관한 피처이므로 제거하였다. CAD 렌더링 시 카메라 거리는 랜덤, 실제 촬영 시 작업자 거리가 일정하지 않으므로, 분류 신호가 없다. 오히려 학습 데이터에서 특정 클래스가 우연히 특정 거리에서 촬영된 경우 shortcut 학습 위험이 있어, NUM_AUX_FEATURES를 4→3으로 줄여 제거하였다.

**[전경 영역 검출]**

SAM(Segment Anything Model)을 활용하여 RGB 이미지에서 전경(부품) 영역을 분리한다. 학습 데이터에 대해서는 SAM2로 사전 생성한 마스크(`mask_NNNN.png`)를 사용하고, 실시간 추론 시에는 MobileSAM(40.7MB)으로 이미지 중심점 프롬프트 기반 마스크를 생성한다. SAM 마스크가 없는 경우, Depth Otsu 이진화 + 형태학적 처리 + 최대 연결 영역 추출 방식으로 폴백한다.

전경 픽셀이 부족할 경우(100개 미만) 안전하게 0벡터를 반환하여 학습 안정성을 보장한다.

**[SAM 적용 전후 AR 일관성 비교]**

SAM 마스크를 적용한 경우와 기존 Depth-only 방식의 종횡비(AR) 일관성을 비교한 결과, SAM 적용 시 AR 표준편차가 크게 감소하여 피처의 안정성이 대폭 향상되었다.

| | SAM 마스크 AR std | Depth-only AR std | 개선 배율 |
|---|---|---|---|
| CAD 데이터 | 0.019~0.035 | 0.112~0.145 | 4~7배 |
| 실제 데이터 | 0.043~0.268 | 0.301~0.510 | 2~10배 |

> **표 2-8c.** SAM 적용 전후 AR 일관성 비교

CAD LH_FRT 그룹의 평균 AR 비교: E25: 1.332, E30: 1.256, E38: 1.200 → 모델 간 명확 구분이 가능하다.

**[모델 아키텍처: RGBDAuxResNet18]**

기존 ResNet18 backbone의 512차원 CNN 특징에, 물리 치수 3개(width, height, aspect_ratio)를 소규모 MLP(Multi-Layer Perceptron)로 처리한 32차원 피처를 연결(concatenate)하여 총 544차원의 복합 특징 벡터를 구성하고, 최종 분류기(FC)에 입력한다.

```
                ┌──────────────────────────┐
                │  RGBD 이미지 (4ch, 448²)  │
                │        ResNet18          │
                │  → 512차원 CNN 특징       │
                └────────────┬─────────────┘
                             │
          ┌──────────────────┼──────────────────┐
          │                  │                  │
          │    ┌─────────────┴─────────────┐    │
          │    │  물리 치수 (3개)            │    │
          │    │  → Linear(3→32) → ReLU   │    │
          │    │  → 32차원 Aux 특징         │    │
          │    └─────────────┬─────────────┘    │
          │                  │                  │
          │           Concatenate               │
          │         [512 + 32 = 544]             │
          │                  │                  │
          │    ┌─────────────┴─────────────┐    │
          │    │  Dropout(0.3)              │    │
          │    │  FC(544→256)+ReLU          │    │
          │    │  Dropout(0.2)              │    │
          │    │  FC(256→N_class)           │    │
          │    └───────────────────────────┘    │
          └────────────────────────────────────┘
```

> **그림 2-3b.** RGBDAuxResNet18 모델 아키텍처

이 접근법의 핵심 이점은 CNN이 학습하기 어려운 **절대적 물리 크기 정보**를 명시적 수치로 제공함으로써, 시각적으로 거의 동일한 형상의 부품도 40~50mm 크기 차이에 기반하여 정확히 분류할 수 있다는 점이다. 특히 SAM 기반 전경 분리와 3D PCA를 결합함으로써, 카메라 시점이 달라져도 일관된 물리 치수를 추출할 수 있어 보조 피처의 신뢰성이 크게 향상되었다.

---

### 2.3 데이터 증강 기법 및 모델 경량화 기술 조사

#### 2.3.1 데이터 증강 기법

실물 데이터셋(1,256장)의 한계를 극복하기 위해, 온라인(학습 시 실시간)과 오프라인(데이터 생성 시) 두 가지 방식의 데이터 증강 기법을 적용하였다.

**[온라인 증강 기법 (학습 시 실시간 적용)]**

매 에포크(epoch)마다 학습 이미지에 랜덤 변환을 적용하여 실질적 데이터 다양성을 확대한다. RGB와 Depth 간의 기하학적 일관성을 유지하기 위해, 공간 변환은 동일한 파라미터로 양쪽에 적용하고 색상·노이즈 변환은 RGB에만 적용한다.

| 증강 기법 | 적용 대상 | 파라미터 | 확률 | 목적 | RGBD 동기화 |
|----------|----------|---------|------|------|-----------|
| HorizontalFlip | RGB + Depth | 좌우 반전 | 50% | 좌우 시점 다양성 | 동일 반전 적용 |
| RandomRotation | RGB + Depth | ±5° | 100% | 회전 불변성 확보 | 동일 각도 적용 |
| RandomScale | RGB + Depth | 0.9~1.1배 | 50% | 카메라 거리 변화 대응 | 동일 스케일 적용 |
| AdjustBrightness | RGB만 | factor: 0.6~1.4 | 100% | 밝기 변화 대응 | Depth 미적용 |
| AdjustContrast | RGB만 | factor: 0.6~1.4 | 100% | 대비 변화 대응 | Depth 미적용 |
| AdjustSaturation | RGB만 | factor: 0.7~1.3 | 100% | 채도 변화 대응 | Depth 미적용 |
| AdjustHue | RGB만 | ±0.05 | 100% | 색조 변화 대응 | Depth 미적용 |
| GaussianNoise | RGB만 | σ: 3~10 | 30% | 센서 노이즈 대응 | Depth 미적용 |
| GaussianBlur | RGB만 | kernel: 3 | 20% | 촬영 포커스 변화 대응 | Depth 미적용 |

> **표 2-9.** 온라인 데이터 증강 기법

> **참고**: Depth 채널은 물리적 거리값이므로 색상·노이즈·블러 증강을 적용하면 실제 거리 정보가 왜곡된다. 따라서 이들 증강은 RGB 3채널에만 적용한다. 공간 변환(반전, 회전, 스케일) 시 Depth에는 `INTER_NEAREST` 보간법을 사용하여 거리값의 보간에 의한 왜곡을 방지한다. 수평 반전(HorizontalFlip)은 도어의 LH_FRT/LH_RR/RH 세 모양이 구조적으로 비대칭(격자 패턴, 힌지 위치, 보강재 등)이므로 클래스 혼동 없이 적용 가능하다.

**[오프라인 증강 기법 (합성 데이터 생성 시 적용)]**

Isaac Sim의 Omni Replicator를 활용한 도메인 랜덤화(Domain Randomization)를 통해, 데이터 생성 단계에서 대규모 오프라인 증강을 수행하였다. 이 방식은 실 환경의 다양한 조건(배경, 조명, 시점)을 시뮬레이션하여 Sim-to-Real 전이 성능을 향상시킨다.

| 랜덤화 항목 | 랜덤화 범위 | 비율/분포 | 목적 |
|------------|-----------|----------|------|
| 배경 | 없음 / 단색 / 팩토리 배경 | 20% / 30% / 50% | 배경 의존성 제거 |
| 카메라 수평 각도 | ±45° | 균등 분포 | 다양한 수평 시점 |
| 카메라 수직 각도 | 15° ~ 75° | 균등 분포 | 다양한 수직 시점 |
| 카메라 거리 | 부품 대각선 기반 | fill 60%, ±8° | 적절한 프레이밍 |
| 조명 방향 | Dome Light 회전 | 연속 분포 | 다양한 그림자 패턴 |

> **표 2-10.** 도메인 랜덤화 항목

<!-- TODO: 도메인 랜덤화 적용 예시 이미지 삽입 (배경 없음/단색/팩토리 비교) -->
<!-- datasets_cad 에서 서로 다른 배경의 샘플 3장 선정 -->

#### 2.3.2 모델 경량화 기술 조사

학습된 모델을 엣지 장비(NVIDIA Jetson Orin)에 배포하기 위한 경량화 기술을 조사·적용하였다.

**[적용 기술 요약]**

| 기술 | 적용 여부 | 상세 | 효과 |
|------|----------|------|------|
| Transfer Learning | ✅ 적용 | ImageNet 사전학습 가중치 활용 | 학습 시간 단축, 소규모 데이터 대응 |
| ResNet18 경량 모델 | ✅ 적용 | 11M 파라미터 (ResNet50의 44%) | 추론 속도 향상 |
| ONNX 변환 | ✅ 적용 | opset 18, 43.13MB | 크로스 플랫폼 호환성 |
| TensorRT 변환 | ⚠️ 시도 | 출력 불일치 문제 발생 | 향후 재시도 예정 |
| 양자화 (INT8/FP16) | 🔲 미적용 | 2차년도 후반 적용 예정 | 추론 속도 2~4배 향상 예상 |

> **표 2-11.** 모델 경량화 기술 적용 현황

**[ONNX 변환]**

학습된 PyTorch 모델을 ONNX(Open Neural Network Exchange) 형식(opset 18)으로 변환하여 플랫폼 독립적인 모델 배포를 지원하였다. 변환된 ONNX 모델의 크기는 약 43.13MB이며, 학습 서버(x86_64)에서 변환 후 추론 장비(aarch64, Jetson Orin)로 이식이 가능하다.

**[TensorRT 변환 시도 및 호환성 분석]**

ONNX 모델을 TensorRT 엔진으로 변환하여 GPU 가속 추론을 시도하였으나, **출력 불일치 문제**가 발생하였다. 동일한 입력에 대해 PyTorch와 TensorRT 엔진의 출력 차이가 최대 11.55에 달하였으며, TensorRT 엔진의 분류 정확도가 100%에서 32.35%로 하락하였다.

| 검증 항목 | PyTorch | TensorRT | 비고 |
|----------|---------|----------|------|
| 입력 데이터 차이 | - | **0** (동일) | 입력은 정확히 일치 |
| 출력 값 차이 (최대) | - | **11.55** | 완전히 다른 출력 |
| 분류 정확도 | 98.53% | 32.35% | 66%p 하락 |
| ONNX 파일 검증 | - | 정상 (가중치 내장) | 파일 자체는 문제 없음 |

> **표 2-12.** PyTorch vs TensorRT 출력 비교

**원인 분석**: Jetson Orin 환경(aarch64, TensorRT 10.3)과 PyTorch 2.10의 ONNX export 간 호환성 문제로 추정된다. 특히 커스텀 fc 레이어 구조(Dropout + Linear + ReLU + Dropout + Linear)가 TRT 최적화 과정에서 잘못 변환된 것으로 분석된다.

**현재 운영 방식**: PyTorch (.pth) 직접 추론으로 전환하여 운영 중이며, CPU 기준 86ms/장의 추론 속도로 0.5초 간격 실시간 추론에 충분한 성능을 확인하였다.

**[추론 속도 최적화 로드맵]**

현재 추론 서버(`04_door_realtime_inference.py`)의 속도를 단계적으로 향상시키기 위한 최적화 방안을 조사·정리하였다.

| 순위 | 최적화 방법 | 예상 추론 시간 | 난이도 | 현황 |
|------|-----------|-------------|--------|------|
| 1 | Jetson GPU 추론 (PyTorch) | ~10ms/장 | 낮음 | Jetson PyTorch wheel 설치 후 1줄 수정 |
| 2 | FP16 반정밀도 양자화 | ~6ms/장 | 낮음 | GPU 추론과 함께 `model.half()` 적용 |
| 3 | ONNX Runtime (CPU) | ~40ms/장 | 중간 | ONNX 변환 완료, 추론 코드 수정 필요 |
| 4 | TensorRT (GPU) | ~3ms/장 | 높음 | 출력 불일치 문제 해결 필요 |
| 5 | INT8 양자화 | ~2ms/장 | 높음 | 캘리브레이션 데이터셋 및 정확도 검증 필요 |

> **표 2-13.** 추론 속도 최적화 로드맵

현재 CPU 86ms/장 → GPU 전환만으로 약 8배(~10ms) 향상이 가능하며, FP16 양자화를 추가 적용하면 ~6ms 수준까지 도달할 수 있다. ResNet18 규모(11M 파라미터)에서 FP16 양자화는 정확도 손실이 거의 없어 안전하게 적용 가능하다. 입력 해상도 축소(448→224)는 E25/E30/E38 간 40~50mm 크기 차이의 구별력이 소멸되므로 적용하지 않는다.

---

## 3. 학습 데이터 수집을 위한 하드웨어 장치 선정

### 3.1 부품 인식을 위한 카메라 및 센서 종류 분석

#### 3.1.1 분석 목적

굴착기 부품 인식 시스템에 적합한 센서를 선정하기 위해, 산업용 영상 센서를 유형별로 분류하고 각각의 특성, 장·단점, 적용 환경을 분석하였다. 본 과제에서는 부품의 외관(RGB)과 형상(Depth)을 동시에 활용하는 RGBD 4채널 기반 분류 모델을 설계하였으므로, RGB와 Depth를 동시에 제공하는 센서가 필수적이다.

#### 3.1.2 센서 유형별 분석

**[(1) RGB 카메라]**

산업용 GigE/USB3 카메라, 웹캠 등 일반 RGB 카메라는 비용이 저렴하고 해상도·프레임 레이트가 높으나, 깊이(Depth) 정보를 제공하지 못하여 3D 위치 추정이 불가능하다. 부품의 2D 분류에는 활용 가능하나, 형상이 유사한 부품(예: E30과 E38 도어의 47mm 크기 차이)의 구분에는 한계가 있다.

| 항목 | 내용 |
|------|------|
| 장점 | 고해상도(4K 이상), 높은 FPS(120+), 저가, 다양한 인터페이스(GigE/USB3/MIPI) |
| 단점 | Depth 정보 미제공, 3D 위치 추정 불가, 형상 기반 분류 한계 |
| 대표 제품 | FLIR Blackfly, Basler ace, Lucid Vision Triton |
| 적합 환경 | 2D 외관 검사, 바코드/QR 인식, 색상 기반 분류 |

> **표 3-1.** RGB 카메라 특성

**[(2) RGB-D 카메라 (깊이 센서 내장)]**

하나의 장치에서 RGB 컬러 이미지와 Depth(깊이) 맵을 동시에 제공하는 센서이다. 깊이 측정 방식에 따라 3가지로 세분화된다.

**(2-a) 스테레오 비전 방식**

두 개의 카메라(좌/우)로 촬영한 이미지의 시차(disparity)를 계산하여 깊이를 추정하는 방식이다. 야외 환경에서도 동작하며, 텍스처가 풍부한 대형 부품의 깊이 추정에 적합하다. 다만, 텍스처가 없는 매끈한 금속 표면에서는 시차 계산이 어려워 정확도가 저하될 수 있다.

| 항목 | 내용 |
|------|------|
| 측정 원리 | 좌/우 카메라 시차(disparity) → 삼각 측량 |
| 장점 | 원거리 측정 가능(15m+), 야외 사용 가능, AI 기반 깊이 보정 가능 |
| 단점 | 텍스처 없는 표면에서 정확도 저하, 최소 측정 거리 존재 |
| 대표 제품 | **Stereolabs ZED 시리즈**, Intel RealSense D400 시리즈 |

> **표 3-2.** 스테레오 비전 방식 특성

**(2-b) ToF (Time-of-Flight) 방식**

적외선 광원에서 방출된 빛이 물체에 반사되어 돌아오는 시간을 측정하여 깊이를 계산하는 방식이다. 센서 전체 영역에서 균일한 깊이맵을 제공하며, 텍스처에 무관하게 동작한다. 그러나 태양광 등 강한 적외선 간섭에 취약하여 야외 사용이 제한된다.

| 항목 | 내용 |
|------|------|
| 측정 원리 | IR 광 비행 시간 측정 (위상 차이 방식) |
| 장점 | 텍스처 무관, 균일한 깊이맵, 실시간 처리 가능 |
| 단점 | 태양광 간섭에 취약, 해상도 상대적으로 낮음, 멀티패스 오류 |
| 대표 제품 | Microsoft Azure Kinect, Intel RealSense L515 |

> **표 3-3.** ToF 방식 특성

**(2-c) 구조광 (Structured Light) 방식**

알려진 패턴(점, 줄무늬 등)을 물체에 투사하고, 변형된 패턴을 분석하여 깊이를 계산하는 방식이다. 실내 근거리에서 높은 정확도를 보이나, 야외 환경에서는 투사 패턴이 태양광에 의해 가려져 사용이 어렵다.

| 항목 | 내용 |
|------|------|
| 측정 원리 | 알려진 패턴 투사 → 변형 분석 → 깊이 계산 |
| 장점 | 근거리 고정확도, 미세 형상 측정 가능 |
| 단점 | 야외 사용 불가, 측정 범위 좁음, 반사면 취약 |
| 대표 제품 | Intel RealSense SR300, Orbbec Astra |

> **표 3-4.** 구조광 방식 특성

**[(3) LiDAR 센서]**

레이저 기반의 정밀 거리 측정이 가능하나, 단가가 높고(수백만 원~) RGB 정보를 별도 카메라와 융합해야 하는 제약이 있다. 주로 자율주행 차량, 측량 분야에서 활용되며, 제조 현장의 부품 인식 용도로는 비용 대비 효용이 낮다.

#### 3.1.3 센서 유형 선정 결론

본 과제에서는 부품의 외관(RGB)과 형상(Depth)을 동시에 활용하는 RGBD 4채널 기반 분류 모델을 설계하였으므로, **RGB-D 카메라(스테레오 비전 방식)** 를 핵심 센서로 선정하였다. 스테레오 비전 방식은 제조 현장의 대형 금속 부품 촬영에 적합한 원거리 측정 능력과 야외/실내 겸용 가능성을 갖추고 있으며, 최신 AI 기반 깊이 보정(Neural Depth)을 통해 텍스처 없는 금속 표면의 한계를 극복할 수 있다.

---

### 3.2 주요 제품 스펙 비교를 통한 최적 장비 선정

#### 3.2.1 RGB-D 카메라 제품 비교

스테레오 비전 및 ToF 방식의 대표 RGB-D 카메라 3종의 주요 스펙을 비교 분석하였다.

| 비교 항목 | ZED X Mini | Intel RealSense D455 | Microsoft Azure Kinect |
|----------|-----------|---------------------|----------------------|
| **깊이 측정 방식** | 스테레오 비전 | 스테레오 비전 + IR | ToF + RGB |
| **RGB 해상도** | **1920×1200** | 1280×800 | 3840×2160 |
| **Depth 해상도** | **1920×1200** | 1280×720 | 640×576 |
| **Depth 범위** | **0.1 ~ 15m** | 0.6 ~ 6m | 0.25 ~ 5.46m |
| **FPS** | 30 / 60 | 30 / 90 | 30 |
| **인터페이스** | **GMSL2** | USB-C 3.1 | USB-C 3.0 |
| **AI 깊이 추정** | **Neural Depth 지원** | 미지원 | 미지원 |
| **방진방수** | **IP66** | 없음 | 없음 |
| **Jetson 호환** | **네이티브 지원** | 별도 드라이버 필요 | 미지원 |
| **SDK 언어** | Python, C++, C# | Python, C++, C# | C, C++ |
| **무게** | 약 60g | 약 122g | 약 440g |
| **가격대** | 약 $500 | 약 $250 | 약 $400 (단종) |

> **표 3-5.** RGB-D 카메라 3종 스펙 비교

<!-- TODO: 카메라 3종 실물 사진 또는 제품 이미지 삽입 -->

#### 3.2.2 선정 결과: Stereolabs ZED X Mini

종합적인 비교 분석 결과, **Stereolabs ZED X Mini**를 최적 장비로 선정하였다. 선정 근거는 다음과 같다.

| 순위 | 선정 근거 | 상세 설명 |
|------|---------|----------|
| 1 | GMSL2 인터페이스 | NVIDIA Jetson 플랫폼과 네이티브 연동, USB 대역폭 제한 없음 |
| 2 | Neural Depth 모드 | AI 기반 깊이 추정으로 텍스처 없는 금속면/반사면의 깊이 측정 한계 극복 |
| 3 | IP66 방진방수 | 제조 현장 환경(분진, 오일, 습기)에서 장기 운영 가능 |
| 4 | 높은 동시 해상도 | RGB·Depth 모두 1920×1200, 정밀한 부품 형상 인식에 유리 |
| 5 | Python SDK (pyzed) | 빠른 프로토타이핑 및 Flask 웹 서버와 연동 용이 |

> **표 3-6.** ZED X Mini 선정 근거

#### 3.2.3 컴퓨팅 장비 선정

데이터 수집·학습·추론의 각 단계에 최적화된 컴퓨팅 장비를 선정하였다.

| 용도 | 장비 | 주요 사양 | 역할 |
|------|------|---------|------|
| 학습 서버 | GPU 워크스테이션 | NVIDIA RTX 5090 (32GB VRAM), CUDA 12.4 | 대규모 데이터셋 고속 학습 |
| 추론 장비 | NVIDIA Jetson Orin + ZED Box Mini | Jetson Orin (ARM), ZED X Mini 카메라 내장 | 현장 실시간 추론 (86ms/장) |
| 시뮬레이션 서버 | GPU 워크스테이션 | NVIDIA GPU, Isaac Sim 구동 | CAD 합성 데이터 생성 |

> **표 3-7.** 컴퓨팅 장비 선정

<!-- TODO: 전체 하드웨어 구성도 사진 또는 다이어그램 삽입 -->
<!-- (학습 서버 - 네트워크 - Jetson+ZED Box Mini 구성) -->

```
┌─────────────────────────────────────────────────────────────────┐
│                     하드웨어 시스템 구성도                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  [학습 서버]              [네트워크]         [추론 장비]          │
│  ┌──────────────┐         ┌─────┐       ┌──────────────────┐   │
│  │ RTX 5090 GPU │◄───────►│ LAN │◄─────►│ Jetson Orin      │   │
│  │ CUDA 12.4    │         └─────┘       │  + ZED Box Mini  │   │
│  │ Ubuntu 22.04 │                       │  + ZED X Mini    │   │
│  │              │                       │    (GMSL2)       │   │
│  │ · 모델 학습   │                       │                  │   │
│  │ · 평가/분석   │                       │ · 실시간 추론     │   │
│  │ · ONNX 변환   │         .pth          │ · 데이터 수집     │   │
│  │              │─────────전송──────────►│ · 웹 서버 (5001) │   │
│  └──────────────┘                       └──────────────────┘   │
│         │                                                       │
│         │ SSH                                                   │
│         ▼                                                       │
│  [시뮬레이션 서버]                                                │
│  ┌──────────────┐                                               │
│  │ Isaac Sim     │                                               │
│  │ · CAD→USD 변환│                                               │
│  │ · 합성 데이터  │                                               │
│  │   생성 (4000장)│                                               │
│  └──────────────┘                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

> **그림 3-1.** 하드웨어 시스템 구성도

---

## 4. 학습 데이터 수집을 위한 서버 및 DBMS 환경 구축

### 4.1 카메라/센서 연동을 위한 신호처리 소프트웨어 설계

#### 4.1.1 설계 목적

ZED X Mini 카메라로부터 RGB+Depth 영상 신호를 실시간으로 수집·처리·저장하기 위한 소프트웨어 시스템을 설계·구현하였다. 제조 현장에서 비전문 작업자도 웹 브라우저를 통해 데이터를 수집할 수 있도록 Flask 기반 웹 인터페이스를 제공하며, 수집 과정에서 품질 필터링과 프레임 추출을 자동화하여 효율적인 데이터셋 구축을 지원한다.

#### 4.1.2 시스템 아키텍처

전체 시스템은 **카메라 하드웨어 레이어**, **신호처리 레이어**, **웹 서비스 레이어**의 3계층으로 구성된다.

```
┌─────────────────────────────────────────────────────────────────────┐
│                    데이터 수집 시스템 아키텍처                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │                    웹 서비스 레이어 (Flask)                      │ │
│  │  Port 5000                                                     │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │ │
│  │  │ MJPEG    │ │ 녹화/    │ │ 이미지   │ │ 데이터셋        │  │ │
│  │  │ 스트리밍 │ │ 스냅샷   │ │ 선별 UI  │ │ 현황 대시보드   │  │ │
│  │  │          │ │ API      │ │          │ │                  │  │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘  │ │
│  └───────────────────────────┬────────────────────────────────────┘ │
│                              │                                      │
│  ┌───────────────────────────┼────────────────────────────────────┐ │
│  │              신호처리 레이어 (CameraManager)                     │ │
│  │                           │                                     │ │
│  │  ┌──────────────┐  ┌─────┴──────┐  ┌────────────────────────┐  │ │
│  │  │ 프레임 캡처   │  │ 품질 필터링 │  │ RGB+Depth 쌍 동기 저장 │  │ │
│  │  │ (비동기 스레드)│  │ (Laplacian │  │ (8-bit + 16-bit PNG)  │  │ │
│  │  │              │  │  Variance)  │  │                        │  │ │
│  │  └──────────────┘  └────────────┘  └────────────────────────┘  │ │
│  └───────────────────────────┬────────────────────────────────────┘ │
│                              │                                      │
│  ┌───────────────────────────┼────────────────────────────────────┐ │
│  │           카메라 하드웨어 레이어 (ZED SDK)                       │ │
│  │                           │                                     │ │
│  │  ┌──────────────┐  ┌─────┴──────┐  ┌────────────────────────┐  │ │
│  │  │ ZED X Mini   │  │ Neural     │  │ 좌표 단위: mm          │  │ │
│  │  │ (GMSL2)      │  │ Depth 모드 │  │ HD1080, 30fps          │  │ │
│  │  │              │  │            │  │                        │  │ │
│  │  └──────────────┘  └────────────┘  └────────────────────────┘  │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

> **그림 4-1.** 데이터 수집 시스템 3계층 아키텍처

#### 4.1.3 핵심 모듈 상세

**[CameraManager 모듈]**

카메라 프레임 캡처, 녹화, 프레임 추출, 블러 필터링을 통합 관리하는 핵심 모듈이다. 스레드 기반 비동기 캡처로 웹 서버의 요청 처리와 카메라 프레임 획득을 분리하여, UI 응답성을 유지하면서 연속적인 프레임 캡처를 수행한다.

| 기능 | 메서드 | 설명 |
|------|--------|------|
| 초기화 | `__init__()` | ZED SDK 초기화 (HD1080, 30fps, Neural Depth), OpenCV 폴백 |
| 프레임 캡처 | `_capture_loop()` | 백그라운드 스레드에서 RGB+Depth 동기화 캡처 |
| 프레임 획득 | `get_frame()` / `get_depth()` | 최신 RGB/Depth 프레임 반환 (thread-safe) |
| 녹화 시작 | `start_recording()` | N 프레임 간격 자동 추출, 블러 필터링 적용 |
| 녹화 종료 | `stop_recording()` | 추출 결과 통계 반환 (총 프레임, 추출 프레임) |
| 수동 캡처 | `snapshot()` | 현재 프레임 1장 즉시 저장 |

> **표 4-1.** CameraManager 주요 기능

**[신호처리 흐름]**

```
ZED SDK grab()
    │
    ├─► retrieve_image(VIEW.LEFT) ──► BGRA→BGR 변환 ──► RGB 프레임
    │
    └─► retrieve_measure(MEASURE.DEPTH) ──► NaN/Inf→0 치환
                                           ──► float32→uint16 변환
                                           ──► Depth 프레임 (mm)
    │
    ├─► 녹화 모드 시: N 프레임마다 블러 검사
    │       │
    │       ├─► Laplacian Variance ≥ 100 → 저장 (양호)
    │       └─► Laplacian Variance < 100 → 폐기 (흐릿)
    │
    └─► 저장: RGB → frame_NNNN.png (8-bit)
              Depth → frame_depth_NNNN.png (16-bit, mm)
```

> **그림 4-2.** 신호처리 흐름도

**[품질 필터링 — Laplacian Variance 기반 블러 감지]**

비디오 스트리밍 방식으로 프레임을 추출할 때, 카메라 이동 중 발생하는 모션 블러(motion blur) 이미지를 자동 제거하기 위해 Laplacian Variance 기법을 적용하였다. 이 기법은 이미지의 2차 미분(Laplacian)을 계산한 후 그 분산(variance)을 구하여, 분산이 낮을수록(=고주파 성분이 적을수록) 흐릿한 이미지로 판단한다.

| 파라미터 | 값 | 설명 |
|---------|-----|------|
| 커널 | Laplacian (3×3) | cv2.Laplacian(gray, cv2.CV_64F) |
| 지표 | Variance | Laplacian 결과의 분산값 |
| 임계값 | 100 | 100 미만 = 흐릿 → 자동 폐기 |
| 적용 시점 | 프레임 추출 시 | 녹화 중 N 프레임 간격으로 적용 |

> **표 4-2.** 블러 필터링 파라미터

#### 4.1.4 웹 제어 인터페이스 (RESTful API)

Flask 기반 RESTful API를 설계하여, 웹 브라우저에서 데이터 수집 전 과정을 제어할 수 있도록 하였다.

| 엔드포인트 | 메서드 | 설명 | 요청 파라미터 | 응답 |
|-----------|--------|------|-------------|------|
| `/` | GET | 메인 UI 페이지 | - | HTML |
| `/video_feed` | GET | MJPEG 실시간 스트리밍 | - | multipart/x-mixed-replace |
| `/api/start_recording` | POST | 녹화 시작 | class_name, interval | {status, message} |
| `/api/stop_recording` | POST | 녹화 정지 | - | {extracted_count, frame_count} |
| `/api/snapshot` | POST | 수동 1장 캡처 | class_name | {filename, blur_score} |
| `/api/save_selected` | POST | 선별 이미지 저장 | selected_files[] | {saved_count} |
| `/api/dataset_status` | GET | 수집 현황 | - | {classes: [{name, count}]} |
| `/api/classes` | GET | 클래스 목록 | - | {classes: [string]} |

> **표 4-3.** RESTful API 설계

<!-- TODO: 데이터 수집 웹 UI 스크린샷 삽입 -->
<!-- http://localhost:5000 접속 화면 캡처 -->

#### 4.1.5 OpenCV 폴백 지원

ZED SDK가 설치되지 않은 개발 환경에서도 시스템을 테스트할 수 있도록, OpenCV 웹캠 기반 폴백(fallback)을 구현하였다. 폴백 모드에서는 RGB 프레임만 제공되며 Depth는 None을 반환한다. 학습 파이프라인에서는 Depth 부재 시 0으로 채워(zero-fill) graceful degradation을 지원한다.

---

### 4.2 수집된 신호 데이터의 효율적 저장/관리를 위한 DBMS 스키마 설계

#### 4.2.1 설계 배경 및 목적

부품 인식 AI 시스템의 운영 과정에서 발생하는 데이터(이미지, 메타데이터, 학습 이력, 평가 결과)가 지속적으로 증가함에 따라, 체계적이고 효율적인 데이터 관리 체계가 필요하다. 본 연구에서는 대용량 이미지 바이너리 파일은 파일시스템에 저장하고, 메타데이터·학습 이력·평가 결과는 관계형 데이터베이스(RDBMS)로 관리하는 **하이브리드 저장 구조**를 설계하였다.

#### 4.2.2 설계 원칙

| 원칙 | 설명 |
|------|------|
| 하이브리드 저장 | 이미지는 파일시스템, 메타데이터는 RDBMS — 저장 효율과 관리 편의성 동시 확보 |
| 이력 관리 | 촬영 세션 → 이미지 → 학습 → 평가의 전체 생애주기 추적 |
| 확장성 | 새로운 부품 클래스, 센서, 모델 아키텍처 추가 시 스키마 변경 최소화 |
| 데이터 무결성 | 외래 키 제약 및 CHECK 제약을 통한 일관성 보장 |

> **표 4-4.** DBMS 스키마 설계 원칙

#### 4.2.3 ER 다이어그램

전체 데이터베이스는 9개 테이블로 구성되며, 데이터셋·클래스·이미지의 핵심 데이터 테이블과 촬영·학습·평가의 이력 관리 테이블로 나뉜다.

```
                          ┌──────────────────┐
                          │    datasets      │
                          │──────────────────│
                          │ PK id            │
                          │ name             │
                          │ type (real/      │
                          │   synthetic/     │
                          │   mixed)         │
                          │ num_classes      │
                          │ total_images     │
                          │ base_path        │
                          │ created_at       │
                          └────────┬─────────┘
                                   │ 1:N
                    ┌──────────────┼──────────────┐
                    │              │              │
                    ▼              ▼              ▼
          ┌─────────────┐ ┌───────────────┐ ┌──────────────────┐
          │   classes   │ │capture_sessions│ │training_sessions │
          │─────────────│ │───────────────│ │──────────────────│
          │ PK id       │ │ PK id         │ │ PK id            │
          │ FK dataset  │ │ FK dataset    │ │ FK dataset       │
          │ name        │ │ camera_type   │ │ FK model         │
          │ model_name  │ │ capture_method│ │ optimizer        │
          │ part_type   │ │ frame_interval│ │ learning_rate    │
          │ image_count │ │ blur_threshold│ │ batch_size       │
          │ cad_avail   │ │ total_frames  │ │ best_accuracy    │
          └──────┬──────┘ │ valid_frames  │ │ total_time_sec   │
                 │        └───────────────┘ └────────┬─────────┘
                 │ 1:N                               │ 1:N
                 ▼                          ┌────────┼────────┐
          ┌──────────────┐                  ▼        ▼        ▼
          │   images     │        ┌──────────┐ ┌──────────┐ ┌────────────┐
          │──────────────│        │training  │ │evaluation│ │augmentation│
          │ PK id        │        │_metrics  │ │_results  │ │_configs    │
          │ FK class     │        │──────────│ │──────────│ │────────────│
          │ FK session   │        │ PK id    │ │ PK id    │ │ PK id      │
          │ rgb_path     │        │ FK sess  │ │ FK sess  │ │ FK sess    │
          │ depth_path   │        │ epoch    │ │ FK model │ │ name       │
          │ blur_score   │        │ train_   │ │ accuracy │ │ applied_to │
          │ data_source  │        │   loss   │ │ precision│ │ parameters │
          │ is_valid     │        │ val_loss │ │ recall   │ │   (JSON)   │
          │ split        │        │ val_acc  │ │ f1_macro │ │            │
          └──────────────┘        └──────────┘ │ confus.  │ └────────────┘
                                               │   matrix │
          ┌──────────────┐                     │ infer_ms │
          │   models     │─────────────────────└──────────┘
          │──────────────│         1:N
          │ PK id        │
          │ name         │
          │ architecture │
          │ in_channels  │
          │ weights_path │
          │ onnx_path    │
          │ trt_path     │
          └──────────────┘
```

> **그림 4-3.** 데이터베이스 ER 다이어그램

#### 4.2.4 테이블 구성 상세

**[핵심 데이터 테이블 (3개)]**

| 테이블 | 역할 | 주요 컬럼 | 레코드 규모 |
|--------|------|---------|-----------|
| `datasets` | 데이터셋 단위 관리 | name, type(real/synthetic/mixed), num_classes, base_path | ~10건 |
| `classes` | 클래스 정보 관리 | name, model_name(E25/E30/E38), part_type, cad_available | ~30건 |
| `images` | 개별 이미지 메타데이터 | rgb_path, depth_path, blur_score, data_source, split, is_valid | ~10,000건 |

> **표 4-5.** 핵심 데이터 테이블

**[이력 관리 테이블 (4개)]**

| 테이블 | 역할 | 주요 컬럼 | 레코드 규모 |
|--------|------|---------|-----------|
| `capture_sessions` | 촬영 세션 이력 | camera_type, capture_method, frame_interval, total/valid_frames | ~50건 |
| `training_sessions` | 학습 세션 이력 | optimizer, lr, batch_size, best_accuracy, total_time_sec, gpu | ~30건 |
| `training_metrics` | 에포크별 학습 지표 | epoch, train_loss, val_loss, val_accuracy, learning_rate | ~2,000건 |
| `evaluation_results` | 평가 결과 | accuracy, precision/recall/f1, confusion_matrix(JSON), infer_ms | ~50건 |

> **표 4-6.** 이력 관리 테이블

**[보조 테이블 (2개)]**

| 테이블 | 역할 | 주요 컬럼 | 레코드 규모 |
|--------|------|---------|-----------|
| `models` | 학습된 모델 관리 | architecture, in_channels, weights/onnx/trt_path, input_size | ~10건 |
| `augmentation_configs` | 증강 설정 | name, applied_to(rgb/depth/both), parameters(JSON) | ~30건 |

> **표 4-7.** 보조 테이블

#### 4.2.5 핵심 테이블 DDL

**[datasets 테이블]**

```sql
CREATE TABLE datasets (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    name            VARCHAR(200) NOT NULL UNIQUE,
    type            VARCHAR(20)  NOT NULL
                    CHECK (type IN ('real', 'synthetic', 'mixed')),
    description     TEXT,
    num_classes     INTEGER NOT NULL DEFAULT 0,
    total_images    INTEGER NOT NULL DEFAULT 0,
    base_path       VARCHAR(500) NOT NULL,
    background_mode VARCHAR(20),
    created_at      TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

**[images 테이블]**

```sql
CREATE TABLE images (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    class_id        INTEGER NOT NULL REFERENCES classes(id) ON DELETE CASCADE,
    session_id      INTEGER REFERENCES capture_sessions(id),
    rgb_filename    VARCHAR(50)  NOT NULL,
    depth_filename  VARCHAR(50),
    rgb_path        VARCHAR(500) NOT NULL,
    depth_path      VARCHAR(500),
    width           INTEGER NOT NULL,
    height          INTEGER NOT NULL,
    channels        INTEGER NOT NULL DEFAULT 4,
    depth_unit      VARCHAR(10) DEFAULT 'mm',
    depth_bit       INTEGER DEFAULT 16,
    blur_score      REAL,
    data_source     VARCHAR(20) NOT NULL
                    CHECK (data_source IN ('camera', 'isaac_sim', 'augmented')),
    is_valid        BOOLEAN NOT NULL DEFAULT TRUE,
    split           VARCHAR(10) CHECK (split IN ('train', 'test', 'val')),
    captured_at     TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(class_id, rgb_filename)
);
```

#### 4.2.6 인덱스 설계

대량 데이터 테이블의 빈번한 검색 패턴에 대응하는 인덱스를 설계하였다.

```sql
-- 이미지 조회 최적화
CREATE INDEX idx_images_class    ON images(class_id);
CREATE INDEX idx_images_source   ON images(data_source);
CREATE INDEX idx_images_split    ON images(split);
CREATE INDEX idx_images_valid    ON images(is_valid);
CREATE INDEX idx_images_blur     ON images(blur_score);

-- 학습 이력 조회 최적화
CREATE INDEX idx_training_dataset ON training_sessions(dataset_id);
CREATE INDEX idx_training_model   ON training_sessions(model_id);
CREATE INDEX idx_metrics_session  ON training_metrics(session_id);

-- 평가 결과 조회 최적화
CREATE INDEX idx_eval_model      ON evaluation_results(model_id);
CREATE INDEX idx_eval_type       ON evaluation_results(eval_type);
```

#### 4.2.7 현행 파일시스템과의 매핑 관계

현재 파일시스템 기반으로 운영 중인 데이터 관리 체계를 RDBMS로 마이그레이션하기 위한 매핑 관계를 정의하였다.

| 현행 (파일시스템) | DBMS 테이블 | 변환 방식 |
|------------------|------------|----------|
| `dataset_info.json` | `datasets` + `classes` | JSON 파싱 → INSERT |
| `<class>/metadata.json` | `classes` | JSON 파싱 → UPDATE |
| `<class>/rgb_NNNN.png` | `images.rgb_path` | 파일 경로 스캔 → INSERT |
| `<class>/depth_NNNN.png` | `images.depth_path` | 파일 경로 스캔 → UPDATE |
| `artifacts/*.pth` | `models.weights_path` | 경로 등록 |
| `artifacts/evaluation_results.json` | `evaluation_results` | JSON 파싱 → INSERT |
| `artifacts/training_indices.json` | `training_sessions.split_indices_path` | 경로 참조 |

> **표 4-8.** 파일시스템-DBMS 매핑 관계

#### 4.2.8 마이그레이션 로드맵

| 단계 | 내용 | 대상 DBMS | 시기 |
|------|------|---------|------|
| 1단계 | SQLite 기반 메타데이터 DB 구축, 기존 JSON → DB 마이그레이션 | SQLite 3 | 2차년도 Q3 |
| 2단계 | Flask 웹 서버에 DB 연동 (촬영 세션·이미지 자동 등록) | SQLite 3 | 2차년도 Q4 |
| 3단계 | 학습·평가 스크립트에 DB 로깅 통합 | SQLite 3 | 3차년도 Q1 |
| 4단계 | PostgreSQL 전환 및 다중 사용자 환경 지원 | PostgreSQL 15+ | 3차년도 Q2 |

> **표 4-9.** DBMS 마이그레이션 로드맵

> **참고**: 상세 DDL(전체 9개 테이블), 활용 쿼리 예시(6종), 마이그레이션 스크립트 구조는 별도 문서 **`DBMS_SCHEMA_DESIGN.md`** 에 기술하였다.

---

## 5. 부품 인식 AI 학습용 기초 데이터셋 구축

### 5.1 부품 인식 학습을 위한 모사 시험 환경 구축

#### 5.1.1 구축 배경

굴착기 도어 부품은 실물 확보가 제한적이며(종류당 1장), 다양한 시점·조명·배경 조건의 학습 데이터를 대량으로 확보하기 어렵다. 이러한 제약을 극복하기 위해, **NVIDIA Isaac Sim** 기반의 디지털 트윈 모사 시험 환경을 구축하여 CAD 모델로부터 합성 RGBD 데이터를 자동 생성하는 파이프라인을 개발하였다.

#### 5.1.2 CAD 모델 변환 파이프라인

굴착기 도어 8종의 STEP(.stp) CAD 파일을 Isaac Sim이 인식하는 USD(Universal Scene Description) 형식으로 변환하는 자동화 파이프라인을 구축하였다.

```
CAD 모델 변환 파이프라인
══════════════════════════════════════════════════════════

  STEP (.stp)               STL (.stl)               USD (.usd)
  ┌──────────┐   CAD SW    ┌──────────┐   Isaac Sim  ┌──────────┐
  │ E25_door │ ──────────► │ E25_door │ ──────────► │ E25_door │
  │ _LH_FRT  │   변환      │ _LH_FRT  │   Asset     │ _LH_FRT  │
  │ .stp     │             │ .stl     │   Converter  │ .usd     │
  └──────────┘             └──────────┘              └──────────┘
       │                        │                         │
       │    8종 CAD 모델        │    단위: mm              │
       │    E25/E30/E38         │    메시 정리              │ ~/isaac-sim/
       │    × 3종 도어           │                         │ assets/door/
       └────────────────────────┘                         └──────────┘

  ※ E30_door_RH와 E38_door_RH는 동일 부품 → E30_E38_door_RH로 통합
  ※ 카메라 관찰 방향: 클래스별 +Z 또는 -Z 축 메타데이터 정의
```

> **그림 5-1.** CAD 모델 변환 파이프라인

| 클래스 | CAD 파일 | USD 파일 | 관찰 축 | 비고 |
|--------|---------|---------|--------|------|
| E25_door_LH_FRT | E25_door_LH_FRT.stp | E25_door_LH_FRT.usd | +Z | |
| E25_door_LH_RR | E25_door_LH_RR.stp | E25_door_LH_RR.usd | +Z | |
| E25_door_RH | E25_door_RH.stp | E25_door_RH.usd | -Z | |
| E30_door_LH_FRT | E30_door_LH_FRT.stp | E30_door_LH_FRT.usd | +Z | 869×1140mm |
| E30_door_LH_RR | E30_door_LH_RR.stp | E30_door_LH_RR.usd | +Z | |
| E30_E38_door_RH | E30_E38_door_RH.stp | E30_E38_door_RH.usd | -Z | E30/E38 통합 |
| E38_door_LH_FRT | E38_door_LH_FRT.stp | E38_door_LH_FRT.usd | +Z | 916×1140mm |
| E38_door_LH_RR | E38_door_LH_RR.stp | E38_door_LH_RR.usd | +Z | |

> **표 5-1.** 도어 8종 CAD 모델 목록

#### 5.1.2-1 USD 기반 부품 물리 치수 분석

도어 8종 USD 파일의 바운딩 박스를 분석하여 각 부품의 정확한 물리 치수를 측정하였다. OpenUSD(usd-core) 라이브러리의 `UsdGeom.BBoxCache`를 사용하여 월드 좌표계 기준의 정렬된 바운딩 박스(AABB)를 계산하였다.

| 클래스 | 가로(X) mm | 세로(Y) mm | 높이(Z) mm | AR (높이/가로) |
|--------|:---------:|:---------:|:---------:|:----------:|
| E25_door_LH_FRT | 828.0 | 72.7 | 1,140.0 | 1.3768 |
| E25_door_LH_RR | 1,141.0 | 72.0 | 1,140.0 | 0.9991 |
| E25_door_RH | 990.0 | 72.0 | 1,140.0 | 1.1515 |
| E30_door_LH_FRT | **869.0** | 71.3 | 1,140.0 | **1.3119** |
| E30_door_LH_RR | 1,262.0 | 72.0 | 1,140.0 | 0.9033 |
| E30_E38_door_RH | 1,191.0 | 72.0 | 1,140.0 | 0.9572 |
| E38_door_LH_FRT | **916.0** | 71.3 | 1,140.0 | **1.2445** |
| E38_door_LH_RR | 1,456.0 | 72.0 | 1,140.0 | 0.7830 |

> **표 5-1-1.** USD 바운딩 박스 기반 도어 8종 물리 치수

주요 발견 사항은 다음과 같다:

- **세로(Y)와 높이(Z)는 전 클래스에서 거의 동일** (Y≈72mm, Z=1,140mm) — 차이는 오직 **가로(X)** 에만 존재
- **E30_door_LH_FRT(869mm)와 E38_door_LH_FRT(916mm)** 의 가로폭 차이는 **47mm (5.4%)** 로 매우 미세
- **E30_door_LH_RR(1,262mm)와 E38_door_LH_RR(1,456mm)** 의 차이는 **194mm (15.4%)** 로 상대적으로 식별 가능
- E30_door_RH와 E38_door_RH는 동일 부품이므로 이미 E30_E38_door_RH로 통합되어 있음

#### 5.1.3 가상 촬영 환경 구성

Isaac Sim의 Replicator를 활용하여 물리 기반 렌더링(PBR) 환경에서 합성 이미지를 생성하는 가상 촬영 환경을 구성하였다.

| 환경 구성 요소 | 설정값 | 설명 |
|-------------|-------|------|
| 렌더링 엔진 | Isaac Sim Replicator (RTX 기반 PBR) | 물리 기반 고품질 렌더링 |
| 카메라 해상도 | 1024×1024 픽셀 | RGB + Depth 동시 생성 |
| FOV (시야각) | 60° | 표준 시야각 |
| 카메라 배치 | 구면 좌표계 기반 랜덤 위치/각도 | 수평 ±45°, 수직 15~75° |
| 카메라 거리 | 부품 대각선 길이 기반 동적 계산 | fill 60%, ±8° 구도 |
| 조명 | Dome Light (강도 1000, 회전 270°) | 균일한 환경광 |
| RGB 출력 | 8-bit PNG (RGBA → RGB) | 컬러 이미지 |
| Depth 출력 | 16-bit PNG (밀리미터 단위) | 깊이 맵 |
| 클래스당 이미지 수 | 500장 | 총 8클래스 × 500 = 4,000장 |

> **표 5-2.** 가상 촬영 환경 설정

<!-- TODO: Isaac Sim 가상 촬영 환경 스크린샷 삽입 -->
<!-- Isaac Sim에서 도어 USD 모델이 로드된 화면 캡처 -->

#### 5.1.4 도메인 랜덤화 (Domain Randomization)

실 환경으로의 일반화 능력(Sim-to-Real Transfer)을 확보하기 위해, 데이터 생성 시 다음 요소들을 랜덤화하였다.

```
도메인 랜덤화 적용 요소
══════════════════════════════════════════════════════════

                    ┌─── 배경 랜덤화 ──────────────────────┐
                    │  없음 (20%)  │ 단색 (30%) │ 팩토리 (50%) │
                    │  투명 배경    │ 랜덤 색상   │ 공장 텍스처  │
                    └──────────────┴────────────┴──────────────┘
                                      │
              ┌───────────────────────┼──────────────────────┐
              │                       │                      │
    ┌─────────┴───────┐    ┌──────────┴──────────┐   ┌──────┴──────────┐
    │ 카메라 위치      │    │ 카메라 거리         │   │ 조명 방향       │
    │ 수평: ±45°      │    │ fill 60%           │   │ Dome Light     │
    │ 수직: 15°~75°   │    │ ±8° 구도 범위       │   │ 방향 랜덤화    │
    │ 구면좌표 샘플링   │    │ 부품 대각선 기반     │   │                │
    └─────────────────┘    └─────────────────────┘   └────────────────┘
                                      │
                                      ▼
                              ┌───────────────┐
                              │ 렌더링 출력    │
                              │ RGB + Depth   │
                              │ (1024×1024)   │
                              └───────────────┘
```

> **그림 5-2.** 도메인 랜덤화 적용 요소

이 모사 환경을 통해 실물 없이도 대규모 학습 데이터를 확보할 수 있으며, **CAD 사전학습 → 실물 Fine-tuning**의 도메인 적응(Domain Adaptation) 전략이 가능해졌다.

---

### 5.2 다양한 조건을 반영한 다중 형태 데이터 수집 및 전처리

#### 5.2.1 데이터 수집 전략

본 연구에서는 **실물 촬영(Track A)** 과 **CAD 합성(Track B)** 의 두 가지 경로로 다중 형태 RGBD 데이터를 수집하고, 통일된 전처리 파이프라인을 적용하여 모델 학습에 활용하였다.

```
데이터 수집 전략 (이중 경로)
══════════════════════════════════════════════════════════

  Track A: 실물 RGBD 데이터              Track B: CAD 합성 RGBD 데이터
  ┌─────────────────────┐              ┌─────────────────────┐
  │ ZED X Mini 카메라   │              │ NVIDIA Isaac Sim    │
  │ (Neural Depth)      │              │ (Omni Replicator)   │
  │                     │              │                     │
  │ · HD1080, 30fps     │              │ · 1024×1024         │
  │ · Depth: mm 단위    │              │ · Depth: mm 단위    │
  │ · Flask 웹 UI       │              │ · Domain Random.    │
  └──────────┬──────────┘              └──────────┬──────────┘
             │                                    │
             ▼                                    ▼
  ┌─────────────────────┐              ┌─────────────────────┐
  │ datasets/           │              │ datasets_cad/       │
  │ · 8클래스 전체       │              │ · 8종 × 500장       │
  │ · 총 1,256장        │              │ · 총 4,000장        │
  │ · RGB+Depth 쌍      │              │ · RGB+Depth 쌍      │
  └──────────┬──────────┘              └──────────┬──────────┘
             │                                    │
             └────────────────┬───────────────────┘
                              │
                              ▼
                  ┌───────────────────────┐
                  │ 통일 전처리 파이프라인  │
                  │ (depth_utils.py)       │
                  │                       │
                  │ Letterbox 448×448     │
                  │ RGB: ImageNet 정규화   │
                  │ Depth: 5m 클리핑      │
                  │ → [B, 4, 448, 448]    │
                  │ + Depth→물리 치수 계산  │
                  │ → [B, 3] Aux Features │
                  └───────────────────────┘
                              │
                              ▼
                  ┌───────────────────────┐
                  │ RGBDAuxResNet18 학습  │
                  │ (CNN 512d + Aux 32d   │
                  │  = 544d → FC)         │
                  └───────────────────────┘
```

> **그림 5-3.** 이중 경로 데이터 수집 및 통합 전략

#### 5.2.2 실물 RGBD 데이터 수집 (Track A)

ZED X Mini 카메라와 Flask 웹 UI를 통해 실물 도어를 촬영하여 RGBD 데이터셋을 구축하였다.

**[수집 장비 및 환경]**

| 항목 | 설정 |
|------|------|
| 카메라 | ZED X Mini (GMSL2 인터페이스) |
| Depth 모드 | Neural Depth (AI 기반 깊이 추정, mm 단위) |
| 해상도 | 1920×1080 (HD1080) |
| FPS | 30fps |
| 수집 방식 | 비디오 스트리밍 + 프레임 추출 (하이브리드) |
| 프레임 추출 간격 | 5 프레임 (30fps 기준 0.17초) |
| 블러 필터링 | Laplacian Variance, threshold=100 |

> **표 5-3.** 실물 데이터 수집 환경

**[촬영 전략]**

데이터 다양성을 확보하기 위해 체계적인 촬영 전략을 수립하여 적용하였다.

| 다양성 요소 | 촬영 전략 | 범위 |
|------------|----------|------|
| 수평 각도 | 정면, 좌/우 45°, 좌/우 90° | 180° 범위 |
| 수직 각도 | 수평, 상 30°, 하 30° | 60° 범위 |
| 촬영 거리 | 근접(0.5m), 중간(1.5m), 원거리(3m) | 0.5~3m |
| 이동 촬영 | 도어 주위를 천천히 한 바퀴 돌며 녹화 | 360° 연속 |

> **표 5-4.** 촬영 전략

**[수집 현황]**

| 클래스 | 이미지 수 | 해상도 | 데이터 형식 | 비고 |
|--------|-----------|--------|-----------|------|
| E25_door_LH_FRT | 158장 | 1920×1080 | RGB + Depth PNG 쌍 | 촬영 완료 |
| E25_door_LH_RR | 155장 | 1920×1080 | RGB + Depth PNG 쌍 | 촬영 완료 |
| E25_door_RH | 155장 | 1920×1080 | RGB + Depth PNG 쌍 | 촬영 완료 |
| E30_door_LH_FRT | 161장 | 1920×1080 | RGB + Depth PNG 쌍 | 촬영 완료 |
| E30_door_LH_RR | 159장 | 1920×1080 | RGB + Depth PNG 쌍 | 촬영 완료 |
| E30_E38_door_RH | 154장 | 1920×1080 | RGB + Depth PNG 쌍 | E30/E38 통합, 촬영 완료 |
| E38_door_LH_FRT | 154장 | 1920×1080 | RGB + Depth PNG 쌍 | 촬영 완료 |
| E38_door_LH_RR | 160장 | 1920×1080 | RGB + Depth PNG 쌍 | 촬영 완료 |
| **총합** | **1,256장** | | | **8클래스 전체 촬영 완료** |

> **표 5-5.** 실물 데이터 수집 현황

<!-- TODO: 실물 촬영 데이터 샘플 이미지 삽입 (RGB + Depth 쌍, 8종 비교) -->
<!-- datasets/E25_door_LH_FRT/rgb_0000.png, depth_0000.png 등 -->

#### 5.2.3 CAD 합성 RGBD 데이터 생성 (Track B)

Isaac Sim의 Omni Replicator를 활용하여 8종 도어 CAD 모델로부터 대규모 합성 RGBD 데이터를 자동 생성하였다.

| 항목 | 설정 |
|------|------|
| 생성 도구 | NVIDIA Isaac Sim + Omni Replicator |
| 클래스 수 | 8클래스 |
| 클래스당 이미지 수 | 500장 |
| 총 생성량 | **4,000장** RGBD 쌍 |
| RGB 형식 | 8-bit PNG (1024×1024) |
| Depth 형식 | 16-bit PNG (1024×1024, mm 단위) |
| 도메인 랜덤화 | 배경, 카메라 위치/각도/거리, 조명 방향 |

> **표 5-6.** CAD 합성 데이터 생성 설정

<!-- TODO: CAD 합성 데이터 샘플 이미지 삽입 (배경별 비교: 없음/단색/팩토리) -->
<!-- datasets_cad/E25_door_LH_FRT/ 에서 다양한 배경의 샘플 선정 -->

<!-- TODO: 실물 vs 합성 데이터 비교 이미지 삽입 (동일 클래스의 실물/합성 나란히) -->

#### 5.2.4 통일 전처리 파이프라인

실물·합성 데이터 모두 동일한 전처리를 거쳐 모델에 입력된다. 전처리 로직은 `depth_utils.py` 모듈로 공통화하여, 모든 학습·평가·추론 스크립트에서 일관된 처리를 보장한다. 특히, 부품의 종횡비가 분류 핵심 특징이므로 **Letterbox 방식**을 적용하여 장변 기준 축소 + 검정 패딩으로 종횡비를 완전히 보존한다.

| 단계 | RGB 처리 | Depth 처리 | 동기화 |
|------|---------|-----------|--------|
| 1. 로드 | PIL.Image (RGB) | cv2.imread (UNCHANGED, 16-bit) | - |
| 2. Letterbox Resize | 장변 448px, BILINEAR | 장변 448px, INTER_NEAREST | 동일 크기, 종횡비 보존 |
| 3. 중앙 배치 패딩 | 검정(0) 패딩 → 448×448 | 검정(0) 패딩 → 448×448 | 동일 영역, 정보 손실 없음 |
| 4. [학습] HorizontalFlip | np.fliplr (50% 확률) | np.flip axis=1 (50% 확률) | 동일 반전 |
| 5. [학습] Rotation | PIL rotate(±5°) | cv2.warpAffine(±5°, INTER_NEAREST) | 동일 각도 |
| 6. [학습] Scale | cv2.warpAffine(0.9~1.1, 50%) | cv2.warpAffine(0.9~1.1, INTER_NEAREST) | 동일 스케일 |
| 7. [학습] 색상·노이즈 증강 | Brightness/Contrast/Saturation/Hue/Noise/Blur | 미적용 | RGB만 |
| 6. 정규화 | ImageNet mean/std | ÷5000→[0,1]→(x-0.5)/0.25 | 개별 적용 |
| 7. 결합 | torch.cat([RGB_3ch, Depth_1ch]) | → [4, 224, 224] | RGBD 통합 |

> **표 5-7.** RGBD 전처리 파이프라인 단계별 상세

#### 5.2.5 보조 피처(Aspect Ratio) 정확도 분석

Depth + SAM 마스크 기반으로 추출되는 물리 치수 보조 피처(width, height, aspect ratio)의 정확도를 검증하기 위해, USD 기준 Ground Truth AR과 각 데이터셋에서 계산된 AR의 분포를 비교 분석하였다.

**[CAD 합성 데이터 AR 분포]**

| 클래스 | USD 기준 AR | CAD 데이터 평균 AR | 표준편차 | 범위 | 오차 |
|--------|:----------:|:----------------:|:------:|------|:----:|
| E30_door_LH_FRT | 1.3119 | 1.2445 | 0.017 | 1.233~1.256 | 4.95% |
| E38_door_LH_FRT | 1.2445 | 1.1965 | 0.020 | 1.170~1.236 | 4.14% |

> **표 5-8.** CAD 합성 데이터의 AR 분포 (USD 기준 대비)

CAD 데이터에서는 두 클래스의 AR 분포에 미세한 겹침 구간(1.233~1.236)이 존재하나, 전체적으로 분리되는 경향이 있다. 그러나 USD 기준 AR 대비 약 4~5%의 체계적 오차가 관측되며, 이는 카메라 시점 변화에 따른 투영 왜곡에 기인한다.

**[실물 RGBD 데이터 AR 분포]**

| 클래스 | USD 기준 AR | 실물 데이터 평균 AR | 표준편차 | 범위 | 겹침 비율 |
|--------|:----------:|:----------------:|:------:|------|:--------:|
| E30_door_LH_FRT | 1.3119 | 1.3102 | **0.124** | 1.103~1.557 | **100%** |
| E38_door_LH_FRT | 1.2445 | 1.3548 | **0.254** | 1.031~2.038 | **80%** |

> **표 5-9.** 실물 RGBD 데이터의 AR 분포 (USD 기준 대비)

실물 데이터에서는 다음과 같은 심각한 문제가 확인되었다:

1. **표준편차가 CAD 대비 6~12배 증가** — 실제 촬영 환경의 거리/각도/마스크 품질 차이가 주요 원인
2. **두 클래스의 AR 범위가 완전히 겹침** — E30의 전체 샘플(100%)이 겹침 구간 내에 분포
3. **평균 AR 차이(0.045)가 표준편차(0.12~0.25)보다 훨씬 작음** — AR 단독으로는 두 클래스 구분 불가

**[CAD vs 실물 AR 비교]**

| 클래스 | CAD AR (std) | 실물 AR (std) | 노이즈 증가 배율 |
|--------|:-----------:|:------------:|:-----------:|
| E30_door_LH_FRT | 1.2445 ± 0.017 | 1.3102 ± 0.124 | **7.3배** |
| E38_door_LH_FRT | 1.1965 ± 0.020 | 1.3548 ± 0.254 | **12.7배** |

> **표 5-10.** CAD vs 실물 데이터 AR 노이즈 비교

이 분석 결과는 **Depth 기반 물리 치수 보조 피처만으로 E30/E38 LH_FRT를 구분하는 것이 현실적으로 불가능**함을 시사하며, CNN 이미지 특징과의 결합 학습 또는 클래스 병합(E30_E38_door_LH_FRT) 전략의 필요성을 뒷받침한다.

---

### 5.3 데이터 증강(Data Augmentation) 기법 적용을 통한 학습용 데이터 다양성 확보

#### 5.3.1 증강 전략 개요

실물 데이터셋(1,256장)의 한계를 극복하고 모델의 일반화 성능을 향상시키기 위해, **3단계 데이터 증강 전략**을 수립·적용하였다.

```
3단계 데이터 증강 전략
══════════════════════════════════════════════════════════

  ┌──────────────────────────────────────────────────────┐
  │ 1단계: 온라인 증강 (학습 시 실시간)                     │
  │                                                      │
  │  원본 이미지 ──► HFlip/Rotation/Scale ──► 색상·노이즈 증강 │
  │  (매 에포크    (RGB+Depth 동기화)    (RGB만: B/C/S/H/Noise/Blur) │
  │   랜덤 적용)                                          │
  │                                                      │
  │  효과: 실질적 데이터 수량 N배 확대 (매 에포크 다른 변환) │
  └──────────────────────────────────────────────────────┘
                           │
                           ▼
  ┌──────────────────────────────────────────────────────┐
  │ 2단계: 오프라인 증강 (Isaac Sim 도메인 랜덤화)          │
  │                                                      │
  │  CAD 모델 ──► 배경 랜덤화 ──► 카메라 랜덤화 ──► 조명   │
  │               (없음/단색/     (위치/각도/      랜덤화   │
  │                팩토리)        거리)                    │
  │                                                      │
  │  결과: 8클래스 × 500장 = 4,000장 합성 RGBD            │
  └──────────────────────────────────────────────────────┘
                           │
                           ▼
  ┌──────────────────────────────────────────────────────┐
  │ 3단계: 크로스 도메인 전략 (Domain Adaptation)           │
  │                                                      │
  │  CAD 합성 데이터     실물 데이터                        │
  │  (4,000장)          (1,256장, 8종)               │
  │       │                 │                             │
  │       ▼                 ▼                             │
  │  사전학습 ────────► Fine-tuning ────► 최종 모델        │
  │  (부품 형상 학습)    (도메인 적응)     (실환경 배포)     │
  │                                                      │
  │  크로스 도메인 평가: CAD 모델 → 실물 데이터 정량 검증   │
  └──────────────────────────────────────────────────────┘
```

> **그림 5-4.** 3단계 데이터 증강 전략

#### 5.3.2 1단계 — 온라인 증강 (학습 시 실시간)

매 에포크마다 학습 이미지에 랜덤 변환을 적용하여 실질적 데이터 다양성을 확대하였다. RGBD 4채널 데이터의 특성상, 공간 변환은 RGB와 Depth에 동일하게 적용하되 색상 변환은 RGB에만 적용하는 전략을 채택하였다.

| 증강 기법 | 적용 대상 | 파라미터 | 확률 | 목적 | 비고 |
|----------|----------|---------|------|------|------|
| HorizontalFlip | RGB + Depth | 좌우 반전 | 50% | 좌우 시점 다양성 | 동일 반전 적용, 비대칭 부품 안전 |
| RandomRotation | RGB + Depth | ±5° | 100% | 회전 불변성 확보 | 동일 회전 행렬 적용 |
| RandomScale | RGB + Depth | 0.9~1.1배 | 50% | 카메라 거리 변화 시뮬레이션 | 동일 스케일 적용 |
| AdjustBrightness | RGB만 | factor: 0.6~1.4 | 100% | 밝기 변화 대응 | Depth 거리값 보존 |
| AdjustContrast | RGB만 | factor: 0.6~1.4 | 100% | 대비 변화 대응 | Depth 거리값 보존 |
| AdjustSaturation | RGB만 | factor: 0.7~1.3 | 100% | 채도 변화 대응 | Depth 거리값 보존 |
| AdjustHue | RGB만 | ±0.05 | 100% | 색조(색온도) 변화 대응 | Depth 거리값 보존 |
| GaussianNoise | RGB만 | σ: 3~10 | 30% | 센서 노이즈 대응 | Depth 거리값 보존 |
| GaussianBlur | RGB만 | kernel: 3 | 20% | 촬영 포커스 변화 대응 | Depth 거리값 보존 |

> **표 5-8.** 온라인 증강 기법 상세

> **RGBD 동기화 핵심**: Depth 채널은 물리적 거리(mm)를 나타내므로, 색상·노이즈·블러 증강을 적용하면 실제 거리 정보가 왜곡된다. 따라서 이들 증강은 RGB 3채널에만 적용한다. 공간 변환(반전, 회전, 스케일) 시 Depth에는 `INTER_NEAREST` 보간법을 사용하여, 보간에 의한 거리값 왜곡을 방지한다. 수평 반전은 도어 LH_FRT/LH_RR/RH의 내부 구조(격자 패턴, 힌지, 보강재)가 비대칭이므로 클래스 혼동 없이 적용 가능하다.

#### 5.3.3 2단계 — 오프라인 증강 (합성 데이터 도메인 랜덤화)

Isaac Sim의 Replicator를 활용하여 데이터 생성 단계에서 대규모 오프라인 증강을 수행하였다.

| 랜덤화 항목 | 범위/비율 | 적용 방식 | 효과 |
|------------|---------|----------|------|
| 배경 없음 | 20% | 투명 배경 렌더링 | 배경 의존성 제거 |
| 단색 배경 | 30% | 랜덤 RGB 색상 | 배경 색상 다양성 |
| 팩토리 배경 | 50% | 공장 텍스처 적용 | 실 환경 시뮬레이션 |
| 카메라 수평 각도 | ±45° (균등 분포) | 구면 좌표 랜덤 샘플링 | 수평 시점 다양성 |
| 카메라 수직 각도 | 15°~75° (균등 분포) | 구면 좌표 랜덤 샘플링 | 수직 시점 다양성 |
| 카메라 거리 | fill 60%, ±8° | 부품 대각선 기반 계산 | 스케일 다양성 |
| Dome Light 방향 | 연속 분포 | 회전 랜덤화 | 그림자 패턴 다양성 |

> **표 5-9.** 도메인 랜덤화 상세 설정

**생성 결과**: 8클래스 × 500장 = **4,000장**의 도메인 랜덤화된 합성 RGBD 데이터를 확보하였다.

<!-- TODO: 도메인 랜덤화 효과 비교 이미지 삽입 -->
<!-- 동일 도어의 다양한 랜덤화 조건(배경/시점/조명) 샘플 4~6장 그리드 -->

#### 5.3.4 3단계 — 크로스 도메인 전략 (Domain Adaptation)

CAD 합성 데이터와 실물 데이터 간의 도메인 격차(domain gap)를 해소하기 위한 전이학습 전략을 수립하였다.

| 단계 | 데이터 | 목적 | 방법 | 결과 |
|------|--------|------|------|------|
| 사전학습 (Pre-training) | CAD 합성 4,000장 (8종) | 부품 형상의 기본 특징 학습 | RGBDAuxResNet18 전체 학습 | CAD 테스트 99.88% |
| 1차 크로스 도메인 평가 | CAD 모델 → 실물 8종 (1,256장) | 전이 가능성 정량 검증 | `--dataset_dir datasets --use_all` | **14.17%** |
| 2차 크로스 도메인 평가 | 실물 모델 → CAD 8종 (4,000장) | 역방향 전이 검증 | `--dataset_dir datasets_cad --use_all` | **23.35%** |
| 실물 전용 학습 | 실물 1,256장 (8종) | 실 환경 직접 학습 | RGBDAuxResNet18 처음부터 학습 | **100.00%** |

> **표 5-10.** 크로스 도메인 전략 및 실험 결과

**[크로스 도메인 평가 결과 분석]**

양방향 크로스 도메인 평가를 통해, CAD-Real 도메인 간 격차를 정량적으로 확인하였다.

| 평가 방향 | 모델 학습 데이터 | 평가 데이터 | 정확도 | Macro F1 | 오류 패턴 |
|----------|---------------|-----------|--------|----------|----------|
| CAD → Real | CAD 4,000장 | 실물 1,256장 | **14.17%** | 5.44% | 대부분 E30_door_LH_FRT로 편향 |
| Real → CAD | 실물 1,256장 | CAD 4,000장 | **23.35%** | 16.62% | 대부분 E30_E38_door_RH로 편향 |

> **표 5-10b.** 양방향 크로스 도메인 평가 결과

두 방향 모두 랜덤 수준(12.5%) 부근의 극히 낮은 정확도를 보였으며, 이는 **실물 촬영 환경의 조명/배경/텍스처와 CAD 렌더링 환경이 근본적으로 다르기 때문**이다. 각 모델은 부품의 물리적 형상이 아닌, 자신의 훈련 도메인 고유의 시각 패턴(렌더링 스타일, 조명 그림자)을 학습한 것으로 분석된다. 따라서 CAD 사전학습 → 실물 Fine-tuning 전략보다는, **실물 데이터 직접 학습이 현 단계에서 가장 효과적**인 것으로 확인되었다.

#### 5.3.5 증강 효과 검증

**[실물 8종 분류 결과]**

| 지표 | 값 | 비고 |
|------|-----|------|
| 학습 데이터 | 1,004장 (8클래스) | Train 80% |
| 테스트 데이터 | 252장 (8클래스) | Test 20% |
| 테스트 정확도 | **100.00%** (252/252) | 30 에포크에서 최고, 40 에포크 Early Stopping |
| Precision (Macro) | 100.00% | |
| Recall (Macro) | 100.00% | |
| F1-Score (Macro) | 100.00% | |
| 오분류 | 0건 | 전 8클래스 완벽 분류 |
| 학습 시간 | 84.06분 | RTX 5090 (32GB VRAM) |

> **표 5-11.** 실물 8종 분류 최종 성능

<!-- TODO: 혼동 행렬(Confusion Matrix) 이미지 삽입 -->
<!-- artifacts/evaluation_results_door_5090.png -->

**[실시간 추론 파이프라인 검증 결과]**

| 지표 | 값 | 비고 |
|------|-----|------|
| 오프라인 평가 정확도 | **100.00%** (252/252) | PyTorch GPU 추론 (RTX 5090) |
| 클래스 수 | 8클래스 | 전체 도어 종류 |
| 오분류 | 0건 | |
| 추론 엔진 | PyTorch 2.10.0 | GPU/CPU 선택 가능 |
| CPU 추론 시간 | **86 ms/장** | Jetson Orin 환경 기준 |
| GPU 추론 시간 (예상) | **~10 ms/장** | Jetson GPU 전환 시 |

> **표 5-12.** 실시간 추론 파이프라인 검증 결과

**[강한 증강 일반화 실험 (Generalization vs Memorization)]**

모델이 부품의 **형상적 특징(shape)**을 학습했는지, 아니면 특정 개체의 **표면 패턴(스크래치, 도장 상태)**을 암기했는지 검증하기 위해, RGB 이미지에 극단적 증강을 적용하여 표면 질감을 무력화한 뒤 평가하였다. Depth와 Mask는 원본 그대로 유지하여 물리적 형상 정보는 보존하였다.

| 증강 항목 | 파라미터 | 목적 |
|----------|---------|------|
| Brightness | factor 0.2~3.0 | 밝기 극단 변화 |
| Contrast | factor 0.2~3.0 | 대비 극단 변화 |
| Saturation | factor 0.0~3.0 | 채도 완전 제거~과포화 |
| Hue | ±90° (전 스펙트럼) | 색상 완전 변경 |
| Gaussian Noise | sigma 30~60 | 강한 센서 노이즈 |
| Gaussian Blur | kernel 7~15 | 표면 디테일 제거 |

> **표 5-12b.** 극단적 RGB 증강 파라미터 (Depth/Mask는 원본 유지)

| 클래스 | 원본 정확도 | 증강 후 정확도 | 하락폭 |
|--------|:----------:|:----------:|:------:|
| E25_door_LH_FRT | 100.00% | 31.01% (49/158) | -68.99%p |
| E25_door_LH_RR | 100.00% | 12.90% (20/155) | -87.10%p |
| E25_door_RH | 100.00% | 29.68% (46/155) | -70.32%p |
| E30_E38_door_RH | 100.00% | 53.25% (82/154) | -46.75%p |
| E30_door_LH_FRT | 100.00% | 69.57% (112/161) | -30.43%p |
| E30_door_LH_RR | 100.00% | 24.53% (39/159) | -75.47%p |
| E38_door_LH_FRT | 100.00% | 44.16% (68/154) | -55.84%p |
| E38_door_LH_RR | 100.00% | 15.00% (24/160) | -85.00%p |
| **전체** | **100.00%** | **35.03% (440/1256)** | **-64.97%p** |

> **표 5-12c.** 극단적 RGB 증강 후 분류 성능 비교

**[분석 결과]**

전체 정확도가 100% → **35.03%**로 대폭 하락하였다. 랜덤 수준(12.5%)보다는 높으나, 실용 불가 수준이다. 이는 다음을 시사한다:

1. **모델이 RGB 텍스처에 강하게 의존**: Depth와 Mask(물리 치수 보조 피처)가 온전함에도 불구하고 65%p 하락 — 모델의 분류 결정에서 RGB 시각 패턴이 지배적
2. **부분적 형상 인식 존재**: 랜덤(12.5%)보다 약 3배 높은 35%를 유지 — Depth/Aux 피처가 일부 기여하고 있으나 단독으로는 부족
3. **클래스별 편차 존재**: E30_door_LH_FRT(69.57%)는 상대적으로 견고한 반면, E25_door_LH_RR(12.90%)과 E38_door_LH_RR(15.00%)은 거의 랜덤 수준
4. **일반화 한계 확인**: 현재 모델은 특정 개체의 표면 패턴에 상당 부분 의존하므로, **같은 종류의 다른 개체**에 대한 추론 시 정확도 저하가 우려됨

이 결과는 **향후 다중 개체 학습 데이터 확보** 및 **Depth 채널 활용도를 높이는 모델 구조 개선**의 필요성을 뒷받침한다.

**[완화 증강 비교 실험 (Moderate Augmentation)]**

극단적 증강(35.03%)과 원본(100%) 사이의 증강 강도에서 모델의 견고성을 추가 검증하였다. 또한 **마스크 기반 전경 전용 증강**을 통해, 배경 컨텍스트 의존도를 분리 측정하였다.

| 증강 항목 | extreme (35%) | moderate 조정 |
|----------|:-------------:|:------------:|
| Brightness | 0.2~3.0 | **0.5~1.8** |
| Contrast | 0.2~3.0 | **0.5~1.8** |
| Saturation | 0.0~3.0 | **0.2~2.0** |
| Hue | ±90° | **±40°** |
| Noise sigma | 30~60 | **15~30** |
| Blur kernel | 7~15 | **3~9** |

> **표 5-12d.** Moderate 증강 파라미터 비교

| 실험 조건 | 증강 범위 | 증강 강도 | 전체 정확도 | Macro F1 |
|----------|----------|----------|:---------:|:--------:|
| 원본 (datasets) | 없음 | 없음 | **100.00%** | 100.00% |
| datasets_aug | 전경만 (mask_only) | moderate | **94.51%** (1187/1256) | 94.56% |
| datasets_aug2 | 전체 이미지 | moderate | **93.39%** (1173/1256) | 93.48% |
| 이전 extreme | 전체 이미지 | extreme | **35.03%** (440/1256) | 29.16% |

> **표 5-12e.** 증강 강도 및 범위별 정확도 비교

| 클래스 | 원본 | moderate 전경만 | moderate 전체 | extreme 전체 |
|--------|:----:|:--------------:|:------------:|:------------:|
| E25_door_LH_FRT | 100% | 96.20% | 90.51% | 31.01% |
| E25_door_LH_RR | 100% | 86.45% | 85.16% | 12.90% |
| E25_door_RH | 100% | 99.35% | 98.06% | 29.68% |
| E30_E38_door_RH | 100% | 97.40% | 97.40% | 53.25% |
| E30_door_LH_FRT | 100% | 99.38% | 99.38% | 69.57% |
| E30_door_LH_RR | 100% | 95.60% | 88.05% | 24.53% |
| E38_door_LH_FRT | 100% | 92.86% | 96.75% | 44.16% |
| E38_door_LH_RR | 100% | 88.75% | 91.88% | 15.00% |

> **표 5-12f.** 클래스별 증강 조건 비교 상세

**[분석 결과]**

1. **moderate 증강에서의 견고성**: 전체 이미지를 moderate 수준으로 증강해도 93.39%를 유지하며, 모델이 중간 수준의 외관 변화에는 견고함을 확인
2. **배경 의존도 미미**: 마스크 전용(94.51%) vs 전체(93.39%)의 차이가 **1.12%p**에 불과 — moderate 수준에서 배경 변화는 분류 성능에 거의 영향 없음
3. **극단적 증강에서의 급격한 하락**: moderate(93%) → extreme(35%)의 비선형적 성능 저하 — 모델이 RGB 텍스처에 의존하되, 어느 정도의 변형까지는 견딜 수 있는 **임계점**이 존재
4. **취약 클래스 일관성**: E25_door_LH_RR과 E38_door_LH_RR은 모든 조건에서 상대적으로 낮은 정확도 — 이 클래스들의 시각적 구별 특징이 다른 클래스에 비해 취약

**[MobileSAM 통합 실시간 추론]**

실시간 추론 서버(`04_door_realtime_inference.py`)에 MobileSAM을 통합하여, 추론 시에도 SAM 기반 전경 분리 + 3D PCA 물리 치수 계산을 수행한다.

| 항목 | 설정 |
|------|------|
| SAM 모델 | MobileSAM (40.7MB) |
| SAM 추론 시간 | GPU ~10ms |
| SAM 비활성화 | `--no_sam` 옵션으로 비활성화 가능 |
| 폴백 | SAM 마스크 실패 시 Depth Otsu 이진화 방식 자동 적용 |

> **표 5-12b.** MobileSAM 통합 실시간 추론 설정

<!-- TODO: 실시간 추론 UI 스크린샷 삽입 -->
<!-- http://localhost:5001 실시간 추론 화면 캡처 -->

<!-- TODO: 틀린 예측 샘플 이미지 삽입 (E30→E25 오분류 사례) -->

#### 5.3.6 2단계 계층적 분류 전략 실험 및 폐기 (Hierarchical Classification)

CAD 합성 데이터의 특성을 활용한 2단계 계층적 분류 전략을 실험하였다. 8개 클래스를 한 번에 분류하는 대신, 형상이 확연히 다른 3개 모양 그룹(LH_FRT, LH_RR, RH)으로 먼저 분류한 후, 각 그룹 내에서 미세한 크기 차이를 갖는 기종(E25, E30, E38)을 고해상도(960×960) 입력으로 분류하는 방식이다. **그러나 실험 결과 Step 2의 정확도가 매우 낮아, 본 접근법은 폐기하였다.**

**[2단계 분류 구조]**

```
2단계 계층적 분류
══════════════════════════════════════════════════════════

  입력 RGBD 이미지
        │
        ▼
  ┌─────────────────────────────────────────┐
  │ Step 1: 모양 분류 (Shape Classification) │
  │ · 입력: 448×448                          │
  │ · 출력: LH_FRT / LH_RR / RH             │
  │ · 형상 차이가 크므로 높은 정확도 기대     │
  └────────────┬────────────────────────────┘
               │
     ┌─────────┼─────────┐
     ▼         ▼         ▼
  LH_FRT    LH_RR      RH
     │         │         │
     ▼         ▼         ▼
  ┌────────┐ ┌────────┐ ┌────────┐
  │Step 2a │ │Step 2b │ │Step 2c │
  │E25/E30 │ │E25/E30 │ │E25/   │
  │/E38    │ │/E38    │ │E30_E38│
  │960×960 │ │960×960 │ │960×960│
  └────────┘ └────────┘ └────────┘
```

> **그림 5-5b.** 2단계 계층적 분류 구조

| 단계 | 목적 | 입력 크기 | 분류 대상 | 비고 |
|------|------|---------|----------|------|
| Step 1 | 모양(Shape) 분류 | 448×448 | LH_FRT, LH_RR, RH | 3클래스, 형상 차이 뚜렷 |
| Step 2a | LH_FRT 내 기종 분류 | 960×960 | E25, E30, E38 | 가로 차이: 828/869/916mm |
| Step 2b | LH_RR 내 기종 분류 | 960×960 | E25, E30, E38 | 3클래스 |
| Step 2c | RH 내 기종 분류 | 960×960 | E25, E30_E38 | 2클래스 (E30/E38 통합) |

> **표 5-8b.** 2단계 분류 단계별 설정

**[고해상도 입력의 근거]**: Step 2에서 960×960 이상의 고해상도를 사용하는 이유는, 동일 모양(예: LH_FRT) 내에서 E25(828mm), E30(869mm), E38(916mm)의 가로 크기 차이가 40~50mm에 불과하여, 낮은 해상도에서는 픽셀 수준의 차이가 소멸되기 때문이다. 고해상도 입력을 통해 이러한 미세한 비례 차이를 보존할 수 있을 것으로 기대하였다.

**[실험 결과 — 실패]**

CAD 합성 데이터 4,000장(8클래스×500장)으로 2단계 분류를 학습한 결과, Step 1(모양 분류)은 완벽하였으나 Step 2(기종 분류)에서 심각한 성능 저하가 관찰되었다.

| 단계 | 대상 | 정확도 | 비고 |
|------|------|--------|------|
| Step 1 (모양 분류) | LH_FRT / LH_RR / RH | **100.0%** | 형상 차이가 뚜렷하여 완벽 분류 |
| Step 2 LH_FRT | E25 / E30 / E38 | **33.3%** | 3클래스 랜덤 수준, 학습 실패 |
| Step 2 LH_RR | E25 / E30 / E38 | **100.0%** | 유일하게 성공한 그룹 |
| Step 2 RH | E25 / E30_E38 | **69.0%** | 2클래스인데도 부진 |
| **Cascade 최종** | **8클래스** | **67.2%** | 실용 불가 수준 |

> **표 5-8c.** 2단계 계층적 분류 실험 결과 (CAD 데이터)

**[실패 원인 분석]**

1. **픽셀 기반 크기 구별의 한계**: 960×960 해상도에서도 40~50mm의 물리적 크기 차이는 CNN이 학습하기에 너무 미세하다. 도메인 랜덤화로 카메라 거리·각도가 매번 달라지므로, 동일 부품도 이미지상 크기가 크게 변동하여 비례 차이가 희석된다.
2. **보조 피처(Auxiliary Features) 부재의 영향**: 단일 단계 분류(`02_door_cad_classification_5090.py`)에서는 Depth 기반 `compute_aux_features`가 물리 치수(mm)를 수치 피처로 직접 제공하여 40~50mm 차이를 명시적으로 전달한다. 2단계 방식은 이를 CNN 픽셀 학습에만 의존하여 핵심 분류 단서를 활용하지 못했다.
3. **CAD 데이터의 텍스처 부재**: 실물 데이터와 달리 CAD 렌더링은 재질·조명·그림자 패턴의 다양성이 부족하여, 크기 외의 외관 특징이 학습되지 않는다.

**[결론 및 폐기 사유]**

본 실험을 통해, **유사 형상 부품의 크기 차이는 CNN 픽셀 패턴이 아닌 Depth 기반 물리 치수 보조 피처(Auxiliary Features)로 구별하는 것이 유효**함을 확인하였다. 2단계 계층적 분류 스크립트(`02_door_cad_classification_5090_2step.py`)는 `deprecated/` 폴더로 이동하여 보관하며, 향후 개발에서는 단일 단계 분류 + 보조 피처 방식을 유지한다.

#### 5.3.7 실행 파이프라인 종합

전체 데이터셋 구축·학습·평가·배포 파이프라인을 아래와 같이 정리한다.

```
전체 실행 파이프라인
══════════════════════════════════════════════════════════

  Step 0) SAM2 전경 마스크 생성
  05_generate_masks.py --dataset all
  → datasets/*/mask_*.png (1,256장)
  → datasets_cad/*/mask_*.png (4,000장)

  Track A: CAD 합성 데이터                Track B: 실물 데이터
  ──────────────────────                ──────────────────

  Step 1) CAD → USD 변환                Step 4) ZED X Mini 촬영
  00_convert_stl_to_usd.py             01_capture_dataset.py
          │                                     │
          ▼                                     ▼
  Step 2) Isaac Sim 합성 생성           Step 5) 크로스 도메인 평가
  01_generate_door_cad_dataset.py       03_door_cad_evaluation_5090.py
  → datasets_cad/ (4,000장)            → CAD 모델로 실물 8종 분류 검증
          │                              ※ 전체 8클래스 (1,256장)
          ▼                                     │
  Step 3) CAD 모델 학습                 ┌───────┴───────┐
  02_door_cad_classification_5090.py    │               │
  (RGBDAuxResNet18, 448×448)            ▼               ▼
  → best_door_cad_model_5090.pth   Step 6a)        Step 6b)
                                   Fine-tuning     실물 전용 학습
                                   04_door_cad_    02_door_
                                   finetune_real   classification
                                       │          (--full_train 옵션)
                                       │               │
                                       └───────┬───────┘
                                               │
                                               ▼
                                       Step 7) 최종 평가
                                       03_door_class_evaluation_5090.py
                                               │
                                               ▼
                                       Step 8) 실시간 추론 배포
                                       04_door_realtime_inference.py
                                       → http://0.0.0.0:5001
                                       (--model/--list_models/--no_sam 옵션)
                                       MobileSAM 통합 (GPU ~10ms)
```

> **그림 5-5.** 전체 실행 파이프라인

---

## 6. 부록: 산출물 목록

### A. 코드 산출물

| 번호 | 파일 | 역할 | 비고 |
|------|------|------|------|
| 0 | `depth_utils.py` | RGBD 공통 유틸리티 (전처리, RGBDAuxResNet18, SAM+PCA 물리 치수 계산, Dataset, 마스크 로딩) | 모든 스크립트에서 공유 |
| 0 | `camera_utils.py` | ZED X Mini RGB+Depth 캡처 모듈 | OpenCV 폴백 지원 |
| 1 | `00_convert_stl_to_usd.py` | CAD STL → USD 변환 자동화 | Isaac Sim Asset Converter |
| 2 | `01_generate_door_cad_dataset.py` | Isaac Sim CAD 합성 RGBD 데이터 생성 | 클래스당 500장 |
| 0b | `05_generate_masks.py` | SAM2 기반 전경 마스크 일괄 생성 | `--dataset cad/real/all` |
| 3 | `01_capture_dataset.py` | Flask 웹 UI 기반 실물 RGBD 데이터 수집 | 포트 5000 |
| 4 | `02_door_cad_classification_5090.py` | CAD 합성 데이터 RGBD 모델 학습 | RTX 5090 |
| 5 | `02_door_classification_5090.py` | 실물 데이터 RGBD 모델 학습 | RTX 5090, `--full_train` 옵션 |
| 5b | `deprecated/02_door_cad_classification_5090_2step.py` | 2단계 계층적 분류 학습 (Shape→Model) | **폐기** — Step 2 정확도 33% (실패) |
| 6 | `03_door_cad_evaluation_5090.py` | CAD 모델 평가 + 크로스 도메인 평가 | `--use_all` 옵션 |
| 7 | `03_door_class_evaluation_5090.py` | 실물 모델 평가 | 혼동 행렬, F1 |
| 8 | `03_5_trt_evaluation.py` | 추론 파이프라인 사전 검증 | TensorRT 호환성 검증 |
| 9 | `04_door_realtime_inference.py` | 실시간 RGBD 추론 서버 | 포트 5001, `--model`/`--list_models`/`--no_sam` 옵션, MobileSAM 통합 |
| 10 | `04_door_cad_finetune_real_5090.py` | CAD→실물 Fine-tuning | 도메인 적응 |

> **표 A-1.** 코드 산출물 목록

### B. 모델 산출물

| 파일 | 크기 | 설명 |
|------|------|------|
| `best_door_model_5090.pth` | 44MB | 실물 8종 학습 모델 (100% 정확도) |
| `best_door_model_5090.onnx` | 43MB | ONNX 변환 모델 (opset 18) |
| `best_door_cad_model_5090.pth` | 44MB | CAD 8종 학습 모델 |
| `sam_models/mobile_sam.pt` | 39MB | MobileSAM 실시간 세그멘테이션 모델 |
| `sam_models/sam2.1_hiera_small.pt` | 176MB | SAM2.1 오프라인 마스크 생성 모델 |
| `deprecated/best_door_cad_shape_5090.pth` | 44MB | 2단계 분류 Step 1 — **폐기** |
| `deprecated/best_door_cad_{shape}_5090.pth` | 44MB | 2단계 분류 Step 2 — **폐기** |
| `deprecated/depth_utils_bbox_aux.py` | - | 기존 BBox 방식 보조 피처 — **폐기** |
| `deprecated/depth_utils_otsu_pca.py` | - | Otsu+PCA 방식 (SAM 이전) — **폐기** |
| `deprecated/04_door_realtime_inference_no_sam.py` | - | SAM 미통합 추론 서버 — **폐기** |
| `class_names_door_5090.json` | 1KB | 클래스 이름 목록 |
| `training_indices_door_5090.json` | 5KB | Train/Test 분할 정보 |
| `evaluation_results_door_5090.json` | 3KB | 평가 결과 JSON |
| `evaluation_results_door_5090.png` | 500KB | 평가 결과 시각화 |

> **표 B-1.** 모델 산출물 목록

### C. 데이터셋 산출물

| 데이터셋 | 경로 | 이미지 수 | 형식 |
|---------|------|----------|------|
| 실물 RGBD (8종) | `datasets/` | 1,256장 | RGB(8-bit) + Depth(16-bit) PNG |
| CAD 합성 RGBD (8종) | `datasets_cad/` | 4,000장 | RGB(8-bit) + Depth(16-bit) PNG |
| SAM 전경 마스크 (실물) | `datasets/*/mask_*.png` | 1,256장 | 8-bit 이진 마스크 (0/255) |
| SAM 전경 마스크 (CAD) | `datasets_cad/*/mask_*.png` | 4,000장 | 8-bit 이진 마스크 (0/255) |
| **RGBD 총합** | | **5,256장** | |
| **마스크 총합** | | **5,256장** | |

> **표 C-1.** 데이터셋 산출물

### D. 문서 산출물

| 문서 | 경로 | 설명 |
|------|------|------|
| 도어 분류 계획서 | `DOOR_CLASSIFICATION_PLAN_20260326_0841.md` | 전체 설계·진행 이력 |
| CAD 분류 전략 분석 | `DOOR_CAD_CLASSIFICATION_PLAN_20260323_1315.md` | 8클래스 CAD 전략 |
| RGBD 마이그레이션 계획 | `RGBD_MIGRATION_PLAN.md` | RGB→RGBD 전환 계획 |
| DBMS 스키마 설계 | `report/DBMS_SCHEMA_DESIGN.md` | 9개 테이블 DDL, ER 다이어그램 |
| 데이터셋 명세서 | `DATASET_SPECIFICATION.md` | Isaac Sim 데이터셋 형식 정의 |

> **표 D-1.** 문서 산출물

---

---

## 7. 추후 할일 (TODO)

### 7.1 모델 일반화 성능 검증

| 우선순위 | 항목 | 설명 | 예상 난이도 |
|---------|------|------|-----------|
| 1 | 동일 종류 다른 개체 검증 | 같은 클래스의 **다른 실물 부품**에 대한 추론 정확도 검증 (현재 학습/평가 데이터는 각 클래스당 1개 개체에서 촬영) | 중 |
| 2 | 촬영 환경 변동 검증 | 조명, 배경, 카메라 거리/각도가 크게 다른 환경에서의 강건성 테스트 | 중 |
| 3 | 시간 경과 후 재검증 | 부품 표면 상태 변화(산화, 오염 등)에 따른 정확도 변화 모니터링 | 낮 |

### 7.2 모델 성능 개선

| 우선순위 | 항목 | 설명 | 예상 난이도 |
|---------|------|------|-----------|
| 1 | E30/E38 LH_FRT 구분 강화 | 47mm(5.4%) 미세 크기 차이 → 표면 디테일(슬롯, 보강재 패턴) 활용 방안 연구 | 높 |
| 2 | 데이터 증강 고도화 | 더 다양한 촬영 조건 시뮬레이션 (CutMix, MixUp, Style Transfer 등) | 중 |
| 3 | 크로스 도메인 적응 | CAD-Real 도메인 격차 해소를 위한 도메인 적응 기법 (DANN, MMD Loss 등) 적용 | 높 |
| 4 | Backbone 업그레이드 | EfficientNet-B0/B2 등 더 강력한 특징 추출기로 교체 실험 | 중 |

### 7.3 배포 및 운영

| 우선순위 | 항목 | 설명 | 예상 난이도 |
|---------|------|------|-----------|
| 1 | Jetson GPU 추론 전환 | PyTorch CPU → GPU 전환으로 추론 속도 86ms → ~10ms 개선 | 낮 |
| 2 | FP16 양자화 적용 | `model.half()` 적용으로 추론 속도 추가 향상 (~6ms) | 낮 |
| 3 | TensorRT 변환 문제 해결 | ONNX → TensorRT 변환 시 출력 불일치 문제 원인 분석 및 해결 | 높 |
| 4 | DBMS 마이그레이션 | 파일시스템 → SQLite → PostgreSQL 단계적 전환 | 중 |
| 5 | 위치 추정(6DoF Pose) 모듈 개발 | 분류 결과 기반 부품 3D 위치/자세 추정 파이프라인 구축 | 높 |

### 7.4 데이터셋 확장

| 우선순위 | 항목 | 설명 | 예상 난이도 |
|---------|------|------|-----------|
| 1 | 다중 개체 데이터 수집 | 각 클래스별 2~3개 이상의 서로 다른 실물 부품 촬영 | 중 |
| 2 | 부품 종류 확대 | 도어 외 다른 굴착기 부품(카울링, 커버 등) 추가 | 높 |
| 3 | 혼합 학습 데이터셋 구축 | CAD + Real 데이터를 효과적으로 결합하는 학습 전략 개발 | 높 |

---

**문서 작성일**: 2026년 3월 28일  
**최종 수정일**: 2026년 3월 28일 08:00
